# Nivel 1.15 — Lot B : le compteur de l'anneau — Plan d'implémentation

> **Pour les agents :** SOUS-COMPÉTENCE REQUISE — utiliser `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans` pour exécuter ce plan tâche par tâche.

**But :** que quatre chiffres tiennent dans le disque intérieur du double anneau de l'accueil, au lieu de mordre le tracé.

**Architecture :** deux constantes, pas une. Le rembourrage horizontal du bloc central monte de 14 à 24 pt — **c'est lui qui corrige le défaut** — et la taille de police descend de 26 à 22 pt, ce qui achète de la réserve avant le plancher de réduction plutôt que le fait de tenir.

> ⚠️ **Ce plan a été écrit avec une géométrie fausse de 12 pt**, corrigée pendant l'exécution. Le `.padding(6)` de `ring` est appliqué **avant** le `.frame(maxWidth: 130)` : il retranche au lieu d'ajouter, et le `ZStack` ne reçoit que 118 pt. Les bons nombres sont dans la spec §4 et dans le code livré : disque blanc de 82 pt de diamètre (rayon 41), corde utile 73,6 pt, largeur disponible ramenée à 70 pt, marge 3,6 pt. Les extraits de test ci-dessous portent encore les anciens chiffres (102 / 94 / 87 / seuil 87) : **ne pas les recopier**, se référer au code de `NivelTests/CalorieRingCenterTests.swift`, qui calcule la corde depuis les constantes de l'anneau au lieu d'épingler un seuil.

**Pile :** SwiftUI, XCTest.

**Spec :** `docs/superpowers/specs/2026-08-16-nivel-1.15-design.md` §4.

**Dépendance :** aucune. Ce lot est autonome et peut être fait en premier.

---

## ⚠️ Deux choses à savoir avant de commencer

**1. Ne pas toucher au reste du fichier.** `minimumScaleFactor(0.7)`, `lineLimit(1)`, `contentTransition(.numericText())` et le plafond `centerTypeSizeCap = .large` restent **exactement** tels quels. Le plafond de Dynamic Type et le rembourrage règlent deux problèmes différents : le premier empêche le texte de grossir avec les réglages système, le second l'empêche d'être plus large que son cercle. Retirer l'un parce qu'on a ajouté l'autre ferait revenir le défaut par l'autre porte.

**2. Les constantes doivent être `static` pour être testables.** Aujourd'hui les deux valeurs sont écrites en dur dans le `body`. Un test ne peut pas lire une vue SwiftUI ; il peut lire une propriété statique. La tâche les extrait d'abord, les utilise ensuite.

**Commandes de test :**
```bash
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:NivelTests/CalorieRingCenterTests test
```

Aucun fichier créé dans ce lot, donc **aucun `xcodegen generate` nécessaire** — les deux fichiers touchés existent déjà.

---

## Structure des fichiers

| Fichier | Responsabilité | Action |
|---|---|---|
| `App/Views/Home/CalorieRing.swift` | Le double anneau de l'accueil | Modifier : 2 constantes, 1 prévisualisation |
| `NivelTests/CalorieRingCenterTests.swift` | Épinglage des deux constantes | **Créer** |

> `NivelTests/CalorieRingLegendTests.swift` existe déjà et couvre la légende de dépense (`showsBurnLegend`). Ne pas y ajouter ces tests : la légende et le contenu central sont deux décisions indépendantes, et les mélanger rendrait l'échec ambigu.

---

## Task 1 : extraire les deux constantes et les corriger

**Fichiers :**
- Modifier : `App/Views/Home/CalorieRing.swift` (bloc `ring`, autour des lignes 100-120)
- Créer : `NivelTests/CalorieRingCenterTests.swift`

- [ ] **Étape 1 : écrire le test qui échoue**

Créer `NivelTests/CalorieRingCenterTests.swift` :

```swift
// NivelTests/CalorieRingCenterTests.swift
// Le contenu central du double anneau (spec 1.15 §4) : quatre chiffres doivent
// tenir DANS le disque blanc, pas seulement dans le cadre carré de l'anneau.

import XCTest
@testable import Nivel

final class CalorieRingCenterTests: XCTestCase {

    /// Les deux valeurs de la spec 1.15 §4. Épinglées et non bornées : elles ont été
    /// relevées à l'image, et une dérive « juste un point de plus » ramènerait
    /// exactement le défaut qu'on corrige.
    func testLesDeuxConstantesDuCentreSontCellesDeLaSpec() {
        XCTAssertEqual(CalorieRingCard.centerFontSize, 22)
        XCTAssertEqual(CalorieRingCard.centerHorizontalPadding, 24)
    }

    /// Le test qui porte le RAISONNEMENT, et pas seulement les nombres.
    ///
    /// Géométrie, toute en points : l'anneau est plafonné à 130 de large ; le second
    /// anneau est posé avec `.padding(14)`, son cercle fait donc 102 de diamètre et
    /// son trait 8, ce qui laisse un disque blanc de 94. Le bloc de deux lignes
    /// occupe environ 34 de haut, donc son coin le plus éloigné se trouve à 17 du
    /// centre : la demi-corde utile y vaut √(47² − 17²) ≈ 43,8, soit **87 de large**.
    ///
    /// La largeur laissée au texte doit rester sous cette corde. Avec 14 de
    /// rembourrage elle valait 102 : le texte avait le droit d'être plus large que le
    /// cercle censé le contenir, et `minimumScaleFactor` ne le rattrapait qu'après
    /// qu'il ait mordu le tracé.
    func testLaLargeurLaisseeAuTexteResteSousLaCordeDuDisqueInterieur() {
        let largeurDisponible = CalorieRingCard.ringMaxWidth
            - 2 * CalorieRingCard.centerHorizontalPadding
        XCTAssertLessThanOrEqual(largeurDisponible, 87,
                                 "le texte central peut déborder du disque blanc")
    }

    /// Ce que le plafond de Dynamic Type garde, et que ce lot ne touche pas.
    func testLePlafondDeDynamicTypeResteLarge() {
        XCTAssertEqual(CalorieRingCard.centerTypeSizeCap, .large)
    }
}
```

- [ ] **Étape 2 : lancer le test pour le voir échouer**

```bash
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:NivelTests/CalorieRingCenterTests test
```

Attendu : **échec de compilation**, `type 'CalorieRingCard' has no member 'centerFontSize'`. C'est bien un échec : les trois constantes n'existent pas encore.

> Le fichier de test vient d'être créé, donc `xcodegen generate` est nécessaire AVANT ce lancement, sinon l'erreur sera « classe de test introuvable » et non celle attendue.

- [ ] **Étape 3 : extraire les constantes et les corriger**

Dans `App/Views/Home/CalorieRing.swift`, à côté de `centerTypeSizeCap`, ajouter :

```swift
    /// Largeur plafond de l'anneau. Déjà appliquée par le `.frame(maxWidth:maxHeight:)`
    /// de `ring` ; nommée ici pour que le test de géométrie puisse la lire.
    static let ringMaxWidth: CGFloat = 130

    /// Taille du gros chiffre. 26 pt jusqu'à la 1.14 : « ~1 240 » y mordait le tracé.
    /// La borne assumée est QUATRE chiffres (spec 1.15 §4), pas davantage.
    static let centerFontSize: CGFloat = 22

    /// Rembourrage horizontal du bloc central. 14 pt jusqu'à la 1.14, ce qui laissait
    /// 102 pt de large à un texte dont le disque blanc n'en offre que 87 à la hauteur
    /// du gros chiffre. C'est LA cause du débordement, la taille de police ne faisait
    /// qu'en retarder l'apparition. Voir CalorieRingCenterTests pour la géométrie.
    static let centerHorizontalPadding: CGFloat = 24
```

Puis, dans `ring`, remplacer les trois valeurs en dur :

```swift
                Text("~\(eaten.frFormatted)")
                    .font(.system(size: Self.centerFontSize, weight: .heavy, design: .rounded))
```

```swift
            .padding(.horizontal, Self.centerHorizontalPadding)
            .dynamicTypeSize(...Self.centerTypeSizeCap)
```

```swift
        .frame(maxWidth: Self.ringMaxWidth, maxHeight: Self.ringMaxWidth)
```

- [ ] **Étape 4 : lancer le test pour le voir passer**

Même commande qu'à l'étape 2. Attendu : **3 tests, 0 échec**.

- [ ] **Étape 5 : ajouter la prévisualisation du pire cas**

À la fin de `CalorieRing.swift`, après les deux prévisualisations existantes :

```swift
#Preview("Pire cas, 4 chiffres") {
    // La BORNE de la spec 1.15 §4, figée pour qu'on la voie au lieu de la supposer :
    // quatre chiffres des deux côtés, avec le dépassement qui passe l'anneau extérieur
    // en `accent` — c'est la combinaison la plus chargée que le centre puisse afficher.
    CalorieRingCard(eaten: 9_999, target: 9_999, burned: 9_999, burnTarget: 9_999)
        .frame(width: 210, height: 210)
        .padding()
        .background(Theme.background)
}
```

- [ ] **Étape 6 : vérifier la non-régression de l'accueil**

```bash
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

Attendu : **toute la cible verte**. `CalorieRingLegendTests` et `HomeDashboardTests` en particulier ne doivent pas bouger — ce lot ne change ni la légende, ni la logique de l'accueil.

- [ ] **Étape 7 : commit**

```bash
git add App/Views/Home/CalorieRing.swift NivelTests/CalorieRingCenterTests.swift Nivel.xcodeproj
git commit -m "fix(accueil): quatre chiffres tiennent dans le double anneau"
```

---

## Vérification finale du lot

- [ ] `xcodebuild ... test` : cible entière verte.
- [ ] Ouvrir la prévisualisation « Pire cas, 4 chiffres » dans Xcode : les deux lignes tiennent **à l'intérieur** du disque blanc, sans toucher le trait orange.
- [ ] Ouvrir la prévisualisation « Sous l'objectif » : le rendu à trois chiffres reste équilibré, le chiffre n'a pas l'air perdu au milieu.
