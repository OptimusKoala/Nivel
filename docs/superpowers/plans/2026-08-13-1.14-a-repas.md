# Nivel 1.14 — Lot A : Repas — Plan d'implémentation

> **Pour les agents :** SOUS-COMPÉTENCE REQUISE — utiliser `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans` pour exécuter ce plan tâche par tâche. Les étapes sont des cases à cocher (`- [ ]`).

**But :** porter le catalogue d'aliments de 85 à 122 entrées, ajouter une cinquième catégorie « Desserts », et rendre impossible la perte d'un panier de repas par fermeture accidentelle.

**Architecture :** presque tout est de la donnée. La 1.10 a posé qu'un plat est une ligne composée (`MealLine.composed`) dont `MealEstimator` somme les composants ; le `kcalPer100g` du plat parent n'est jamais lu pour une ligne composée. Ajouter un plat = une entrée dans `foods.json` + sa composition dans `compositions.json`. Seuls le sélecteur de catégories (cinq segments ne tiennent plus) et le garde de fermeture touchent du Swift.

**Pile :** Swift 6, SwiftUI, SwiftData, XCTest. Logique pure dans le paquet `NivelCore`, interface dans `App/`.

**Spec :** `docs/superpowers/specs/2026-08-13-nivel-1.14-design.md` §3. Les tableaux de valeurs (ids, kcal, compositions) y sont : **ne pas les recopier ici, les lire là-bas**.

**Commandes de test :**
```bash
cd NivelCore && swift test                     # logique pure, ~0,1 s
cd NivelCore && swift test --filter FoodCatalogTests
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

---

## Structure des fichiers

| Fichier | Responsabilité | Action |
|---|---|---|
| `NivelCore/Sources/NivelCore/Foods.swift` | `FoodItem`, sa catégorie, `FoodCatalog` | Modifier : cas `dessert` |
| `NivelCore/Sources/NivelCore/Resources/foods.json` | Les 122 aliments | Modifier : +37 entrées, 3 changements de catégorie, 1 renommage |
| `NivelCore/Sources/NivelCore/Resources/compositions.json` | Compositions des plats | Modifier : +14 compositions |
| `NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift` | Invariants du catalogue | Modifier : nouveaux comptes et invariants |
| `App/Views/Meals/FoodCatalogView.swift` | Le catalogue à onglets | Modifier : pastilles défilantes |
| `App/Views/Meals/MealLogSheet.swift` | La feuille de saisie | Modifier : garde de fermeture |
| `NivelTests/MealLogSheetDismissTests.swift` | Le garde de fermeture | **Créer** |

---

## Task 1 : la catégorie « Desserts »

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/Foods.swift:7-18`
- Modifier : `App/Views/Meals/FoodCatalogView.swift:10-14`
- Test : `NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift`

- [ ] **Étape 1 : écrire le test qui échoue**

Dans `FoodCatalogTests.swift` :

```swift
func testDessertCategoryExists() {
    XCTAssertEqual(FoodItem.Category.dessert.frLabel, "Desserts")
}

/// Garde-fou : une catégorie ajoutée à l'enum sans être rangée dans `tabOrder`
/// disparaîtrait de l'écran en silence.
func testEveryCategoryIsInTabOrder() {
    XCTAssertEqual(Set(FoodItem.Category.tabOrder), Set(FoodItem.Category.allCases))
}
```

`tabOrder` vit côté app (`FoodCatalogView.swift`), pas dans le paquet. **Déplacer l'extension `FoodItem.Category.tabOrder` de `App/Views/Meals/FoodCatalogView.swift` vers `NivelCore/Sources/NivelCore/Foods.swift`** pour que le test puisse l'atteindre : l'ordre d'affichage est une propriété du catalogue, pas de la vue qui le dessine.

- [ ] **Étape 2 : lancer le test, vérifier qu'il échoue**

Run : `cd NivelCore && swift test --filter FoodCatalogTests`
Attendu : ÉCHEC — `type 'FoodItem.Category' has no member 'dessert'`.

- [ ] **Étape 3 : implémentation minimale**

Dans `Foods.swift`, l'enum devient :

```swift
public enum Category: String, Codable, CaseIterable, Sendable {
    case dish, side, drink, snack, dessert

    public var frLabel: String {
        switch self {
        case .dish: "Plats"
        // « Accompagnements » ne tient pas dans cinq pastilles, et la catégorie
        // contient des ingrédients depuis la 1.10 : le libellé rattrape le contenu.
        case .side: "Ingrédients"
        case .drink: "Boissons"
        case .snack: "Encas"
        case .dessert: "Desserts"
        }
    }

    /// Ordre d'affichage (spec §3.4) : les desserts en dernier, après les encas.
    /// Différent de l'ordre de déclaration, et couvert par un test qui vérifie
    /// qu'aucune catégorie ne manque.
    public static let tabOrder: [Category] = [.dish, .side, .drink, .snack, .dessert]
}
```

Retirer l'extension `tabOrder` de `FoodCatalogView.swift` (son commentaire d'origine part avec elle).

- [ ] **Étape 4 : lancer les tests**

Run : `cd NivelCore && swift test`
Attendu : SUCCÈS, aucune régression sur les 166 existants.

- [ ] **Étape 5 : commit**

```bash
git add NivelCore/Sources/NivelCore/Foods.swift \
        NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift \
        App/Views/Meals/FoodCatalogView.swift
git commit -m "feat(core): cinquième catégorie d'aliments, les desserts"
```

---

## Task 2 : les seize ingrédients

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/Resources/foods.json`
- Test : `NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift`

- [ ] **Étape 1 : écrire le test qui échoue**

```swift
func testNewIngredientsArePresent() throws {
    let catalog = try FoodCatalog.load()
    for id in ["wrap", "pita", "kebab_meat", "cheese_sauce", "white_sauce",
               "nuggets", "fried_chicken", "burrata", "dry_sausage", "olives",
               "crackers", "oats", "noodles_cooked", "soy_sauce", "sausage", "bechamel"] {
        let item = catalog.byID[id]
        XCTAssertNotNil(item, "ingrédient manquant : \(id)")
        XCTAssertEqual(item?.category, .side, "\(id) doit être un ingrédient")
        XCTAssertGreaterThan(item?.kcalPer100g ?? 0, 0, "\(id) sans barème")
    }
}

/// Une unité naturelle n'a de sens qu'avec un poids : `hasUnit` l'exige déjà,
/// mais rien ne l'imposait au JSON.
func testUnitLabelAndGramsGoTogether() throws {
    for item in try FoodCatalog.load().items {
        XCTAssertEqual(item.unitLabel != nil, item.unitGrams != nil, "\(item.id)")
    }
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter FoodCatalogTests`
Attendu : ÉCHEC — « ingrédient manquant : wrap ».

- [ ] **Étape 3 : écrire les seize entrées**

Valeurs exactes : **spec §3.2**. Modèle d'entrée sans unité, à recopier pour les treize concernées :

```json
{
  "id": "wrap",
  "name": "Galette de blé",
  "emoji": "🫓",
  "kcalPer100g": 300,
  "unitLabel": null,
  "unitLabelPlural": null,
  "unitGrams": null,
  "category": "side",
  "tags": [],
  "slots": [],
  "defaultGrams": 100
}
```

Modèle avec unité, pour les trois qui en ont (`nuggets`, `dry_sausage`, `sausage`, `soy_sauce`) :

```json
{
  "id": "nuggets",
  "name": "Nuggets",
  "emoji": "🍗",
  "kcalPer100g": 290,
  "unitLabel": "nugget",
  "unitLabelPlural": null,
  "unitGrams": 18,
  "category": "side",
  "tags": [],
  "slots": [],
  "defaultGrams": 90
}
```

`unitLabelPlural` reste `null` quand le pluriel s'obtient en ajoutant un s ; « c. à soupe » (pour `soy_sauce`) est invariable, donc `"unitLabelPlural": "c. à soupe"`.

- [ ] **Étape 4 : lancer les tests**

Run : `cd NivelCore && swift test`
Attendu : SUCCÈS.

- [ ] **Étape 5 : commit**

```bash
git add NivelCore/Sources/NivelCore/Resources/foods.json \
        NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift
git commit -m "feat(core): seize ingrédients pour les nouveaux plats"
```

---

## Task 3 : les quatorze plats et leurs compositions

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/Resources/foods.json`, `compositions.json`
- Test : `NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift`

- [ ] **Étape 1 : écrire le test qui échoue**

```swift
func testNewDishesHaveResolvableCompositions() throws {
    let catalog = try FoodCatalog.load()
    let newDishes = ["tacos", "kebab", "burrata_tomato", "apero_platter",
                     "fried_chicken_salad", "nuggets_meal", "omelette",
                     "omelette_garnie", "gratin_dauphinois", "gratin_veg",
                     "gratin_pasta", "asian_noodles", "oatmeal_fruit", "breakfast_eggs"]
    for id in newDishes {
        guard let item = catalog.byID[id] else { return XCTFail("plat manquant : \(id)") }
        XCTAssertEqual(item.category, .dish, "\(id)")
        let line = catalog.line(for: item)
        XCTAssertTrue(line.isComposed, "\(id) doit avoir une composition")
        for component in line.components {
            XCTAssertNotNil(catalog.byID[component.itemID],
                            "\(id) référence un ingrédient inconnu : \(component.itemID)")
            XCTAssertGreaterThan(component.grams, 0, "\(id)/\(component.itemID)")
        }
    }
}

/// Deux valeurs témoins : si un barème d'ingrédient bouge, ces deux-là le disent.
func testSpotCheckedEstimates() throws {
    let catalog = try FoodCatalog.load()
    let omelette = catalog.line(for: catalog.byID["omelette"]!)
    XCTAssertEqual(MealEstimator.kcal(lines: [omelette], kcalPer100g: catalog.kcalPer100g), 278)
    let tacos = catalog.line(for: catalog.byID["tacos"]!)
    XCTAssertEqual(MealEstimator.kcal(lines: [tacos], kcalPer100g: catalog.kcalPer100g), 1053)
}

func testBreakfastDishesAreSlotted() throws {
    let catalog = try FoodCatalog.load()
    for id in ["oatmeal_fruit", "breakfast_eggs"] {
        XCTAssertEqual(catalog.byID[id]?.slots, [.breakfast], "\(id)")
    }
}

/// La planche apéro n'est ni un déjeuner ni un dîner : slots vide = proposée partout.
func testAperoPlatterHasNoSlot() throws {
    XCTAssertEqual(try FoodCatalog.load().byID["apero_platter"]?.slots, [])
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter FoodCatalogTests`
Attendu : ÉCHEC — « plat manquant : tacos ».

- [ ] **Étape 3 : écrire les quatorze plats**

Composition et créneaux exacts : **spec §3.3**. Dans `foods.json`, chaque plat suit ce modèle (le `kcalPer100g` d'un plat composé n'est jamais lu par l'estimation — il vaut le total de la composition ramené à 100 g, comme les seize plats existants) :

```json
{
  "id": "tacos",
  "name": "Tacos",
  "emoji": "🌮",
  "kcalPer100g": 265,
  "unitLabel": null,
  "unitLabelPlural": null,
  "unitGrams": null,
  "category": "dish",
  "tags": [],
  "slots": ["lunch", "dinner"],
  "defaultGrams": 390
}
```

Dans `compositions.json` :

```json
"tacos": [
  { "itemID": "wrap", "grams": 100 },
  { "itemID": "kebab_meat", "grams": 120 },
  { "itemID": "fries", "grams": 100 },
  { "itemID": "cheese_sauce", "grams": 50 },
  { "itemID": "lettuce", "grams": 20 }
]
```

`defaultGrams` d'un plat composé vaut la somme de sa composition : il n'est pas utilisé par `line(for:)` quand une composition existe, mais un chiffre incohérent tromperait quiconque lit le fichier.

- [ ] **Étape 4 : lancer les tests**

Run : `cd NivelCore && swift test`
Attendu : SUCCÈS. Si `testSpotCheckedEstimates` échoue, **ne pas ajuster le test** : c'est la composition ou un barème d'ingrédient qui a un écart, et le vérifier est le but de ce test.

- [ ] **Étape 5 : commit**

```bash
git add NivelCore/Sources/NivelCore/Resources/foods.json \
        NivelCore/Sources/NivelCore/Resources/compositions.json \
        NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift
git commit -m "feat(core): quatorze plats, du tacos aux flocons d'avoine"
```

---

## Task 4 : les desserts, et les trois déménagements

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/Resources/foods.json`
- Test : `NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift`

- [ ] **Étape 1 : écrire le test qui échoue**

```swift
func testDessertsAreComplete() throws {
    let catalog = try FoodCatalog.load()
    let desserts = catalog.items.filter { $0.category == .dessert }
    XCTAssertEqual(desserts.count, 10, "sept nouveaux + trois déménagés")
    for id in ["fruit_salad", "cake", "fruit_tart", "choco_mousse",
               "crepe_sugar", "skyr", "greek_yogurt",
               "choco_square", "ice_cream", "compote"] {
        XCTAssertEqual(catalog.byID[id]?.category, .dessert, "\(id)")
    }
}

/// Skyr et yaourt grec sont deux entrées : 35 kcal d'écart aux 100 g.
func testSkyrAndGreekYogurtAreDistinct() throws {
    let catalog = try FoodCatalog.load()
    XCTAssertEqual(catalog.byID["skyr"]?.kcalPer100g, 85)
    XCTAssertEqual(catalog.byID["greek_yogurt"]?.kcalPer100g, 120)
}

/// Deux items s'appelaient « Viennoiserie ». Un nom d'aliment doit être unique :
/// deux entrées homonymes dans une grille de taps sont un piège.
func testNamesAreUnique() throws {
    let names = try FoodCatalog.load().items.map(\.name)
    XCTAssertEqual(Set(names).count, names.count,
                   "noms en doublon : \(Dictionary(grouping: names, by: { $0 }).filter { $0.value.count > 1 }.keys)")
}

func testCatalogueSize() throws {
    XCTAssertEqual(try FoodCatalog.load().items.count, 122)
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter FoodCatalogTests`
Attendu : ÉCHEC sur `testDessertsAreComplete` et `testNamesAreUnique`.

- [ ] **Étape 3 : implémentation**

1. Ajouter les sept desserts (**spec §3.4**), catégorie `"dessert"`, `slots` vide.
2. Changer `"category": "snack"` → `"dessert"` pour `choco_square`, `ice_cream`, `compote`. **Ne pas toucher aux ids** : les repas déjà notés ne stockent que des ids, un renommage d'id les amputerait de leur détail.
3. Renommer le snack `croissant` : `"name": "Croissant ou pain au chocolat"`. Le plat `pastry` garde « Viennoiserie ».

- [ ] **Étape 4 : lancer les tests**

Run : `cd NivelCore && swift test`
Attendu : SUCCÈS, 122 aliments.

- [ ] **Étape 5 : commit**

```bash
git add NivelCore/Sources/NivelCore/Resources/foods.json \
        NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift
git commit -m "feat(core): dix desserts, dont trois qui n'étaient pas des encas"
```

---

## Task 5 : le sélecteur en pastilles

**Fichiers :**
- Modifier : `App/Views/Meals/FoodCatalogView.swift:38-47`

Pas de test unitaire : c'est une substitution de contrôle, sans logique. La vérification est visuelle, via les previews Xcode du fichier et la capture `02-meallog`.

- [ ] **Étape 1 : remplacer le `Picker` segmenté**

Cinq segments ne tiennent pas dans la largeur d'un iPhone, et le Dynamic Type les achève. Le contrôle devient une rangée de pastilles défilantes, **identique aux pastilles de créneau de `MealLogSheet.slotPicker`** — un seul motif de sélection dans la feuille plutôt que deux :

```swift
private var categoryPicker: some View {
    ScrollView(.horizontal, showsIndicators: false) {
        HStack(spacing: 8) {
            ForEach(FoodItem.Category.tabOrder, id: \.self) { candidate in
                Button {
                    withAnimation(.snappy) { category = candidate }
                } label: {
                    Text(candidate.frLabel)
                        .font(.footnote.weight(.semibold))
                        .padding(.horizontal, 13)
                        .padding(.vertical, 9)
                        .background(category == candidate ? Theme.orange : Theme.card,
                                    in: Capsule())
                        .foregroundStyle(category == candidate ? .white : Theme.text)
                }
                .buttonStyle(.plain)
                .accessibilityAddTraits(category == candidate ? [.isSelected] : [])
            }
        }
        // Marge négative puis rembourrage : les pastilles défilent bord à bord
        // sans que la première colle au bord de l'écran.
        .padding(.horizontal, 1)
    }
    .scrollClipDisabled()
}
```

Et dans `body`, `Picker` devient `categoryPicker`. `lockedCategory` continue de masquer entièrement le sélecteur : rien à changer de ce côté.

- [ ] **Étape 2 : vérifier au simulateur**

Run : `xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : compilation et 94 tests app au vert.

Puis ouvrir la preview « Nouveau repas » de `MealLogSheet.swift` dans Xcode et vérifier à la main : les cinq pastilles défilent, « Desserts » est atteignable, la sélection se voit. **Refaire le contrôle en Dynamic Type XXL** (inspecteur de preview) : c'est là que les libellés cassaient.

- [ ] **Étape 3 : commit**

```bash
git add App/Views/Meals/FoodCatalogView.swift
git commit -m "feat(app): les catégories d'aliments passent en pastilles défilantes"
```

---

## Task 6 : la fermeture qui ne jette plus rien

**Fichiers :**
- Modifier : `App/Views/Meals/MealLogSheet.swift`
- Test : `NivelTests/MealLogSheetDismissTests.swift` (**créer**)

Le défaut : la feuille se présente avec `presentationDragIndicator(.visible)` et sans garde. Le glissement que l'indicateur *invite* à faire referme la feuille et jette le panier, sans un mot.

- [ ] **Étape 1 : écrire le test qui échoue**

La décision doit être extraite en fonction pure pour être testable sans interface — même approche que `HomeView.bubbleDecision`, déjà testée dans `HomeDashboardTests`.

`NivelTests/MealLogSheetDismissTests.swift` :

```swift
// NivelTests/MealLogSheetDismissTests.swift
// Garde de fermeture de la feuille de repas (spec v1.14 §3.6) : un panier garni
// ne se jette pas d'un glissement. La décision est pure, la vue ne fait que
// l'appliquer.
import XCTest
import NivelCore
@testable import Nivel

final class MealLogSheetDismissTests: XCTestCase {
    func testEmptyBasketClosesWithoutAsking() {
        XCTAssertFalse(MealLogSheet.guardsDismissal(lines: []))
    }

    func testFilledBasketAsksBeforeClosing() {
        let line = MealLine.simple(MealComponent(itemID: "beer_half", grams: 500))
        XCTAssertTrue(MealLogSheet.guardsDismissal(lines: [line]))
    }

    /// En édition, une entrée existante a déjà ses lignes : fermer sans valider
    /// perdrait les MODIFICATIONS, donc le garde vaut là aussi.
    func testEditingAFilledMealAlsoGuards() {
        let line = MealLine.simple(MealComponent(itemID: "pasta", grams: 300))
        XCTAssertTrue(MealLogSheet.guardsDismissal(lines: [line]))
    }
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:NivelTests/MealLogSheetDismissTests test`
Attendu : ÉCHEC de compilation — `guardsDismissal` n'existe pas.

- [ ] **Étape 3 : implémenter**

Dans `MealLogSheet` :

```swift
/// Vrai quand fermer la feuille détruirait du travail. Pure et testée
/// (MealLogSheetDismissTests) : la vue ne fait que l'appliquer.
static func guardsDismissal(lines: [MealLine]) -> Bool { !lines.isEmpty }

@State private var showsAbandonDialog = false
```

Dans `body`, après `.presentationDragIndicator(.visible)` :

```swift
.interactiveDismissDisabled(Self.guardsDismissal(lines: lines))
```

Dans la `NavigationStack`, une barre d'outils :

```swift
.toolbar {
    // Sortie VISIBLE : dès qu'on empêche le glissement, il faut en offrir une.
    ToolbarItem(placement: .topBarLeading) {
        Button {
            if Self.guardsDismissal(lines: lines) {
                showsAbandonDialog = true
            } else {
                dismiss()
            }
        } label: {
            Image(systemName: "xmark")
                .font(.footnote.weight(.bold))
        }
        .tint(Theme.subtext)
        .accessibilityLabel("Fermer")
    }
}
.confirmationDialog("Abandonner ce repas ?", isPresented: $showsAbandonDialog,
                    titleVisibility: .visible) {
    // PAS de `role: .destructive` : un bouton destructif de confirmationDialog est
    // peint en rouge par le système et refuse d'être teinté (contrairement à une
    // action de glissement, cf. SportView). Un bouton ordinaire est la seule façon
    // de respecter « jamais de rouge » (règle v1 §7.4) dans un dialogue.
    Button("Abandonner") { dismiss() }
    Button("Reprendre", role: .cancel) {}
} message: {
    Text("Ton panier n'est pas encore enregistré.")
}
```

Le titre de l'écran (`Text(isEditing ? "Modifier le repas" : "Nouveau repas")`) reste dans le contenu défilant : le mettre en `navigationTitle` mangerait de la hauteur, ce que la 1.13 a passé son temps à récupérer.

- [ ] **Étape 4 : lancer les tests**

Run : `xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : SUCCÈS, y compris les 94 tests app existants.

- [ ] **Étape 5 : vérifier à la main au simulateur**

Trois gestes, dans cet ordre :
1. Ouvrir « Noter un repas », **panier vide**, glisser vers le bas → la feuille se ferme sans rien demander.
2. Ajouter un plat, glisser vers le bas → **rien ne se passe** (le glissement est bloqué).
3. Taper la croix → le dialogue apparaît, « Reprendre » ramène le panier intact, « Abandonner » ferme.
4. Rouvrir la feuille, ajouter un plat, ouvrir le détail d'une ligne, glisser vers le bas depuis cet écran → bloqué aussi.

- [ ] **Étape 6 : commit**

```bash
git add App/Views/Meals/MealLogSheet.swift NivelTests/MealLogSheetDismissTests.swift
git commit -m "fix(app): un panier de repas ne se jette plus d'un glissement"
```

---

## Task 7 : vérification du lot

- [ ] **Étape 1 : suite complète**

```bash
cd NivelCore && swift test && cd ..
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```
Attendu : tout au vert. Le compte de tests NivelCore a augmenté (166 + les nouveaux).

- [ ] **Étape 2 : tests d'interface du panier**

Hors du schéma quotidien, à lancer parce que `MealLogSheet` a changé :

```bash
xcodebuild -project Nivel.xcodeproj -scheme NivelPreview \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:NivelUITests/BasketSwipeTests test
```
Attendu : SUCCÈS. Le glissement pour retirer une ligne du panier doit continuer de marcher — `interactiveDismissDisabled` ne touche pas les gestes internes, mais c'est précisément le genre d'interaction qu'on croit sans risque.

- [ ] **Étape 3 : relecture de la spec**

Reprendre **spec §3** point par point et cocher : 122 aliments, cinquième catégorie, libellé « Ingrédients », deux entrées d'omelette, trois gratins, skyr et yaourt grec séparés, plus de doublon « Viennoiserie », garde de fermeture sans rouge.

**Ce que le lot A ne fait PAS**, et qu'il ne faut pas ajouter en passant : aucun brouillon persisté (spec §3.6), aucune recette (lot D), aucune modification du widget.
