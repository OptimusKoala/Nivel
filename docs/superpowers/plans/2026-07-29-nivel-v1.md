# Nivel v1 — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** App iOS française "Nivel" (SwiftUI, iOS 17+) d'accompagnement de perte de poids gamifiée pour deux profils indépendants, avec mascotte Nivelito, selon la spec `docs/superpowers/specs/2026-07-29-nivel-v1-design.md`.

**Architecture:** Un package Swift `NivelCore` contient toute la logique métier pure (calculs caloriques, estimation repas, XP/niveaux, quêtes, badges, messages) avec ses catalogues JSON en ressources — testé en TDD via `swift test` (rapide, sans simulateur). L'app SwiftUI (target générée par XcodeGen via `project.yml`) contient les modèles SwiftData, les services système (HealthKit, notifications, clôture de journée) et les vues. Nivelito est dessiné en `Path` SwiftUI d'après `design/nivelito.svg`.

**Tech Stack:** Swift 6 / SwiftUI / SwiftData / HealthKit / UserNotifications / Swift Charts / XcodeGen 2.46 / XCTest. Aucune dépendance tierce.

**Conventions générales :**
- Tout le texte visible utilisateur est en **français**. Code, identifiants et commits en anglais.
- Environnement : Xcode 26.6, simulateur de vérification `iPhone 17 Pro`, cible `iOS 17.0`.
- Commande de test core : `cd NivelCore && swift test` (macOS, pas besoin de simulateur — les sources NivelCore ne doivent importer que `Foundation`).
- Commande de build app : `xcodegen generate && xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build` (à lancer depuis la racine).
- Design tokens (Theme) : fond crème `#fdf6ec`, cartes blanches, texte `#5b4a3f`, texte secondaire `#b09a8a`, orange principal `#f57c1f`, accent `#f5b453`, vert `#7bc86c`, bleu `#4da3c7`, piste de jauge `#f4e7db`, contour Nivelito `#3a1220`. Rayons 24 pt (cartes) / 22 pt (boutons). Police système `.rounded`. **Jamais de rouge.**

---

## Carte des fichiers

```
Nivel/
├── project.yml                          # XcodeGen — targets Nivel + NivelTests
├── README.md                            # dont procédure re-build 7 jours
├── design/nivelito.svg                  # (existe) référence mascotte
├── design/appicon.svg                   # icône app (Nivelito sur fond crème)
├── NivelCore/                           # Package Swift — logique pure
│   ├── Package.swift
│   ├── Sources/NivelCore/
│   │   ├── Models.swift                 # Sex, ActivityLevel, MealSlot, Portion, value types
│   │   ├── CalorieCalculator.swift      # BMR / TDEE / objectif
│   │   ├── MealEstimator.swift          # estimation kcal d'un repas
│   │   ├── StepsEstimator.swift         # kcal estimées des pas
│   │   ├── LevelSystem.swift            # seuils de niveaux, progression
│   │   ├── XPEngine.swift               # attribution d'XP + plafonds quotidiens
│   │   ├── QuestEngine.swift            # tirage hebdo + progression des quêtes
│   │   ├── BadgeEngine.swift            # évaluation des badges
│   │   ├── MessageBank.swift            # sélection des messages Nivelito
│   │   ├── Catalogs.swift               # structs Codable + chargement des JSON
│   │   └── Resources/
│   │       ├── dishes.json              # plats + kcal
│   │       ├── extras.json              # desserts/boissons + kcal
│   │       ├── quests.json              # pool de quêtes hebdo
│   │       ├── badges.json              # définitions des badges
│   │       └── messages.json            # banque de messages Nivelito
│   └── Tests/NivelCoreTests/
│       ├── CalorieCalculatorTests.swift
│       ├── MealEstimatorTests.swift
│       ├── LevelSystemTests.swift
│       ├── XPEngineTests.swift
│       ├── QuestEngineTests.swift
│       ├── BadgeEngineTests.swift
│       ├── MessageBankTests.swift
│       └── CatalogsTests.swift          # intégrité des 5 JSON
├── App/
│   ├── NivelApp.swift                   # @main, ModelContainer, scenePhase → DayCloser
│   ├── Theme.swift                      # couleurs, styles, modifiers partagés
│   ├── RootView.swift                   # splash → onboarding ou tabs
│   ├── Models/PersistentModels.swift    # @Model: UserProfile, MealEntry, WeightEntry, DayLog, GamificationState
│   ├── Services/
│   │   ├── HealthKitService.swift       # protocole StepsProviding + impl HK + fake
│   │   ├── NotificationService.swift    # planification des 4 rappels
│   │   ├── DayCloser.swift              # rattrapage au premier plan (spec §9)
│   │   └── GameService.swift            # façade: XP, level-up, badges, quêtes ← NivelCore
│   ├── Nivelito/
│   │   ├── NivelitoShapes.swift         # Paths traduits du SVG
│   │   ├── NivelitoView.swift           # expressions + animations (idle, joie, dodo…)
│   │   └── SpeechBubble.swift
│   ├── Views/
│   │   ├── Onboarding/OnboardingFlow.swift
│   │   ├── Home/HomeView.swift          # + CalorieRing, StatCard, XPBar
│   │   ├── Meals/MealLogSheet.swift     # flux 3 étapes
│   │   ├── Meals/MealsJournalView.swift
│   │   ├── Progress/ProgressView.swift  # + WeightChart, CaloriesChart, StepsChart, WeighInSheet
│   │   ├── Quests/QuestsView.swift      # quêtes + badges
│   │   ├── Settings/SettingsView.swift
│   │   └── Celebrations/LevelUpView.swift  # + ConfettiView
│   └── Assets.xcassets                  # AppIcon
└── NivelTests/AppSmokeTests.swift       # tests légers côté app (DayCloser)
```

Répartition : chaque moteur de NivelCore est un type pur sans état global, injectable et testable. L'app ne contient aucune formule métier — elle orchestre.

---

## Task 1 : Squelette du package NivelCore

**Files:** Create: `NivelCore/Package.swift`, `NivelCore/Sources/NivelCore/Models.swift`, `NivelCore/Tests/NivelCoreTests/CalorieCalculatorTests.swift` (vide pour l'instant)

- [ ] **Step 1 : Créer le manifest**

```swift
// NivelCore/Package.swift
// swift-tools-version:5.9
import PackageDescription

let package = Package(
    name: "NivelCore",
    platforms: [.iOS(.v17), .macOS(.v14)],
    products: [.library(name: "NivelCore", targets: ["NivelCore"])],
    targets: [
        .target(name: "NivelCore", resources: [.process("Resources")]),
        .testTarget(name: "NivelCoreTests", dependencies: ["NivelCore"]),
    ]
)
```

- [ ] **Step 2 : Créer les types de base**

```swift
// NivelCore/Sources/NivelCore/Models.swift
import Foundation

public enum Sex: String, Codable, CaseIterable, Sendable {
    case male, female
}

public enum ActivityLevel: String, Codable, CaseIterable, Sendable {
    case sedentary, light, moderate, active

    public var factor: Double {
        switch self {
        case .sedentary: 1.2
        case .light: 1.375
        case .moderate: 1.55
        case .active: 1.725
        }
    }
}

public enum MealSlot: String, Codable, CaseIterable, Sendable {
    case breakfast, lunch, dinner, snack

    /// Créneau pré-rempli d'après l'heure (spec §4.2) :
    /// avant 11 h = petit-déj, 11-15 h = déjeuner, après 18 h = dîner, sinon encas.
    public static func suggested(forHour hour: Int) -> MealSlot {
        switch hour {
        case ..<11: .breakfast
        case 11..<15: .lunch
        case 18...: .dinner
        default: .snack
        }
    }
}

public enum Portion: String, Codable, CaseIterable, Sendable {
    case light, normal, hearty

    public var multiplier: Double {
        switch self {
        case .light: 0.7
        case .normal: 1.0
        case .hearty: 1.3
        }
    }
}
```

- [ ] **Step 3 : Vérifier que le package compile et que la suite (vide) passe**

Run: `cd NivelCore && swift test`
Expected: `Test Suite 'All tests' passed` (0 tests) — et aucun warning de ressource (le dossier `Resources/` n'existe pas encore ; créer `Resources/.gitkeep` si SwiftPM râle, il sera rempli en Task 3).

- [ ] **Step 4 : Commit**

```bash
git add NivelCore && git commit -m "feat: NivelCore package skeleton with base value types"
```

---

## Task 2 : CalorieCalculator (TDD)

**Files:** Create: `NivelCore/Sources/NivelCore/CalorieCalculator.swift` — Test: `NivelCore/Tests/NivelCoreTests/CalorieCalculatorTests.swift`

Formules (spec §6) : Mifflin-St Jeor — homme `10×poids + 6.25×taille − 5×âge + 5`, femme `… − 161`. TDEE = BMR × facteur. Objectif = TDEE − 350, arrondi à la dizaine la plus proche, plancher 1200 (femme) / 1500 (homme).

- [ ] **Step 1 : Écrire les tests qui échouent**

```swift
// NivelCore/Tests/NivelCoreTests/CalorieCalculatorTests.swift
import XCTest
@testable import NivelCore

final class CalorieCalculatorTests: XCTestCase {
    // Michaël-type : homme, 90 kg, 180 cm, 40 ans
    func testBMRMale() {
        let bmr = CalorieCalculator.bmr(sex: .male, weightKg: 90, heightCm: 180, ageYears: 40)
        XCTAssertEqual(bmr, 10*90 + 6.25*180 - 5*40 + 5, accuracy: 0.001) // 1830
    }

    func testBMRFemale() {
        let bmr = CalorieCalculator.bmr(sex: .female, weightKg: 70, heightCm: 165, ageYears: 38)
        XCTAssertEqual(bmr, 10*70 + 6.25*165 - 5*38 - 161, accuracy: 0.001) // 1380.25
    }

    func testTDEEAppliesActivityFactor() {
        let tdee = CalorieCalculator.tdee(bmr: 1830, activity: .light)
        XCTAssertEqual(tdee, 1830 * 1.375, accuracy: 0.001)
    }

    func testDailyTargetSubtracts350AndRoundsToTen() {
        // TDEE 2516.25 → 2166.25 → arrondi 2170
        let target = CalorieCalculator.dailyTarget(sex: .male, weightKg: 90, heightCm: 180, ageYears: 40, activity: .light)
        XCTAssertEqual(target, 2170)
    }

    func testDailyTargetFloorFemale() {
        // Profil très petit → sous 1200 → plancher
        let target = CalorieCalculator.dailyTarget(sex: .female, weightKg: 45, heightCm: 150, ageYears: 60, activity: .sedentary)
        XCTAssertEqual(target, 1200)
    }

    func testDailyTargetFloorMale() {
        let target = CalorieCalculator.dailyTarget(sex: .male, weightKg: 50, heightCm: 155, ageYears: 70, activity: .sedentary)
        XCTAssertEqual(target, 1500)
    }
}
```

- [ ] **Step 2 : Vérifier l'échec** — Run: `cd NivelCore && swift test` — Expected: FAIL, `cannot find 'CalorieCalculator'`.

- [ ] **Step 3 : Implémenter**

```swift
// NivelCore/Sources/NivelCore/CalorieCalculator.swift
import Foundation

public enum CalorieCalculator {
    public static func bmr(sex: Sex, weightKg: Double, heightCm: Double, ageYears: Int) -> Double {
        let base = 10 * weightKg + 6.25 * heightCm - 5 * Double(ageYears)
        return sex == .male ? base + 5 : base - 161
    }

    public static func tdee(bmr: Double, activity: ActivityLevel) -> Double {
        bmr * activity.factor
    }

    /// Objectif quotidien : TDEE − 350, arrondi à la dizaine, avec plancher de sécurité (spec §6).
    public static func dailyTarget(sex: Sex, weightKg: Double, heightCm: Double, ageYears: Int, activity: ActivityLevel) -> Int {
        let tdee = tdee(bmr: bmr(sex: sex, weightKg: weightKg, heightCm: heightCm, ageYears: ageYears), activity: activity)
        let raw = tdee - 350
        let rounded = Int((raw / 10).rounded()) * 10
        let floor = sex == .female ? 1200 : 1500
        return max(rounded, floor)
    }
}
```

- [ ] **Step 4 : Vérifier le succès** — Run: `cd NivelCore && swift test` — Expected: PASS (6 tests).

- [ ] **Step 5 : Commit** — `git add NivelCore && git commit -m "feat: calorie calculator (Mifflin-St Jeor, gentle deficit, safety floors)"`

---

## Task 3 : Catalogues plats/extras + MealEstimator (TDD)

**Files:** Create: `NivelCore/Sources/NivelCore/Catalogs.swift`, `NivelCore/Sources/NivelCore/MealEstimator.swift`, `NivelCore/Sources/NivelCore/Resources/dishes.json`, `.../extras.json` — Test: `MealEstimatorTests.swift`, `CatalogsTests.swift`

- [ ] **Step 1 : Écrire les JSON**

`dishes.json` — champs `id`, `name` (français), `emoji`, `kcal`, `slots` (créneaux où le plat est proposé en premier ; tous les plats restent accessibles) :

```json
[
  {"id": "pasta",        "name": "Pâtes",                    "emoji": "🍝", "kcal": 650, "slots": ["lunch", "dinner"]},
  {"id": "rice",         "name": "Riz / féculents",          "emoji": "🍚", "kcal": 550, "slots": ["lunch", "dinner"]},
  {"id": "salad",        "name": "Salade composée",          "emoji": "🥗", "kcal": 350, "slots": ["lunch", "dinner"]},
  {"id": "veggies",      "name": "Légumes + protéine",       "emoji": "🥦", "kcal": 450, "slots": ["lunch", "dinner"]},
  {"id": "red_meat",     "name": "Viande rouge + accomp.",   "emoji": "🥩", "kcal": 700, "slots": ["lunch", "dinner"]},
  {"id": "fish",         "name": "Poisson + accomp.",        "emoji": "🐟", "kcal": 500, "slots": ["lunch", "dinner"]},
  {"id": "pizza",        "name": "Pizza",                    "emoji": "🍕", "kcal": 900, "slots": ["lunch", "dinner"]},
  {"id": "soup",         "name": "Soupe",                    "emoji": "🍲", "kcal": 300, "slots": ["lunch", "dinner"]},
  {"id": "sandwich",     "name": "Sandwich",                 "emoji": "🥪", "kcal": 550, "slots": ["lunch", "snack"]},
  {"id": "stew",         "name": "Plat mijoté",              "emoji": "🍛", "kcal": 600, "slots": ["lunch", "dinner"]},
  {"id": "fast_food",    "name": "Fast-food",                "emoji": "🍔", "kcal": 950, "slots": ["lunch", "dinner"]},
  {"id": "toast",        "name": "Tartines",                 "emoji": "🍞", "kcal": 350, "slots": ["breakfast"]},
  {"id": "cereal",       "name": "Céréales",                 "emoji": "🥣", "kcal": 400, "slots": ["breakfast"]},
  {"id": "pastry",       "name": "Viennoiserie",             "emoji": "🥐", "kcal": 300, "slots": ["breakfast", "snack"]},
  {"id": "yogurt_fruit", "name": "Yaourt & fruits",          "emoji": "🍓", "kcal": 200, "slots": ["breakfast", "snack"]},
  {"id": "other",        "name": "Autre",                    "emoji": "🍽️", "kcal": 600, "slots": ["breakfast", "lunch", "dinner", "snack"]}
]
```

`extras.json` — champs `id`, `name`, `emoji`, `kcal`, `category` (`dessert` | `drink`) :

```json
[
  {"id": "dessert_light",   "name": "Dessert léger",    "emoji": "🍎", "kcal": 150, "category": "dessert"},
  {"id": "dessert_rich",    "name": "Dessert gourmand", "emoji": "🍰", "kcal": 300, "category": "dessert"},
  {"id": "water",           "name": "Eau",              "emoji": "💧", "kcal": 0,   "category": "drink"},
  {"id": "beer",            "name": "Bière",            "emoji": "🍺", "kcal": 150, "category": "drink"},
  {"id": "wine",            "name": "Vin (verre)",      "emoji": "🍷", "kcal": 120, "category": "drink"},
  {"id": "soda",            "name": "Soda",             "emoji": "🥤", "kcal": 140, "category": "drink"}
]
```

- [ ] **Step 2 : Tests d'intégrité des catalogues + estimateur (échec d'abord)**

```swift
// CatalogsTests.swift — vérifie chargement et unicité des ids
import XCTest
@testable import NivelCore

final class CatalogsTests: XCTestCase {
    func testDishesLoadAndIdsAreUnique() throws {
        let dishes = try Catalogs.dishes()
        XCTAssertGreaterThanOrEqual(dishes.count, 16)
        XCTAssertEqual(Set(dishes.map(\.id)).count, dishes.count)
        XCTAssertTrue(dishes.contains { $0.id == "toast" && $0.slots.contains(.breakfast) })
    }

    func testExtrasLoad() throws {
        let extras = try Catalogs.extras()
        XCTAssertTrue(extras.contains { $0.id == "water" && $0.kcal == 0 })
    }
}
```

```swift
// MealEstimatorTests.swift
import XCTest
@testable import NivelCore

final class MealEstimatorTests: XCTestCase {
    let pasta = Dish(id: "pasta", name: "Pâtes", emoji: "🍝", kcal: 650, slots: [.lunch, .dinner])
    let beer = Extra(id: "beer", name: "Bière", emoji: "🍺", kcal: 150, category: .drink)
    let dessert = Extra(id: "dessert_rich", name: "Dessert gourmand", emoji: "🍰", kcal: 300, category: .dessert)

    func testPortionMultiplierAppliesToDishOnly() {
        // pâtes copieux + 2 bières + dessert : 650×1.3 + 300 + 150×2 = 845 + 600 = 1445
        let kcal = MealEstimator.estimate(dish: pasta, portion: .hearty,
                                          extras: [(dessert, 1), (beer, 2)])
        XCTAssertEqual(kcal, 1445)
    }

    func testLightPortionRounds() {
        // 650×0.7 = 455
        XCTAssertEqual(MealEstimator.estimate(dish: pasta, portion: .light, extras: []), 455)
    }
}
```

- [ ] **Step 3 : Vérifier l'échec** — `cd NivelCore && swift test` — FAIL (`Catalogs`, `Dish`, `MealEstimator` inconnus).

- [ ] **Step 4 : Implémenter**

```swift
// Catalogs.swift
import Foundation

public struct Dish: Codable, Identifiable, Hashable, Sendable {
    public let id: String
    public let name: String
    public let emoji: String
    public let kcal: Int
    public let slots: [MealSlot]
    public init(id: String, name: String, emoji: String, kcal: Int, slots: [MealSlot]) {
        self.id = id; self.name = name; self.emoji = emoji; self.kcal = kcal; self.slots = slots
    }
}

public struct Extra: Codable, Identifiable, Hashable, Sendable {
    public enum Category: String, Codable, Sendable { case dessert, drink }
    public let id: String
    public let name: String
    public let emoji: String
    public let kcal: Int
    public let category: Category
    public init(id: String, name: String, emoji: String, kcal: Int, category: Category) {
        self.id = id; self.name = name; self.emoji = emoji; self.kcal = kcal; self.category = category
    }
}

public enum Catalogs {
    static func load<T: Decodable>(_ file: String) throws -> T {
        guard let url = Bundle.module.url(forResource: file, withExtension: "json") else {
            throw CocoaError(.fileNoSuchFile)
        }
        return try JSONDecoder().decode(T.self, from: Data(contentsOf: url))
    }
    public static func dishes() throws -> [Dish] { try load("dishes") }
    public static func extras() throws -> [Extra] { try load("extras") }
}
```

```swift
// MealEstimator.swift
import Foundation

public enum MealEstimator {
    /// kcal = plat × portion (arrondi) + somme des extras × quantité (spec §6).
    public static func estimate(dish: Dish, portion: Portion, extras: [(Extra, Int)]) -> Int {
        let dishKcal = Int((Double(dish.kcal) * portion.multiplier).rounded())
        let extrasKcal = extras.reduce(0) { $0 + $1.0.kcal * $1.1 }
        return dishKcal + extrasKcal
    }
}
```

- [ ] **Step 5 : Vérifier le succès** — `cd NivelCore && swift test` — PASS. (Le `.gitkeep` éventuel de Task 1 peut être supprimé dès le Step 1 de cette tâche, les JSON occupant désormais `Resources/`.)

- [ ] **Step 6 : Commit** — `git commit -am "feat: dish/extra catalogs and meal calorie estimator"`

---

## Task 4 : StepsEstimator + LevelSystem (TDD)

**Files:** Create: `StepsEstimator.swift`, `LevelSystem.swift` — Test: `LevelSystemTests.swift` (+ 1 test StepsEstimator dedans)

- [ ] **Step 1 : Tests**

```swift
import XCTest
@testable import NivelCore

final class LevelSystemTests: XCTestCase {
    func testThresholds() {
        XCTAssertEqual(LevelSystem.xpRequired(forLevel: 1), 0)      // niveau de départ
        XCTAssertEqual(LevelSystem.xpRequired(forLevel: 2), 246)    // 100×2^1.3 = 246.2 → 246
        XCTAssertEqual(LevelSystem.xpRequired(forLevel: 5), 810)    // 100×5^1.3 = 810.33 → 810
        XCTAssertEqual(LevelSystem.xpRequired(forLevel: 10), 1995)  // 100×10^1.3
    }

    func testLevelForXP() {
        XCTAssertEqual(LevelSystem.level(forXP: 0), 1)
        XCTAssertEqual(LevelSystem.level(forXP: 245), 1)
        XCTAssertEqual(LevelSystem.level(forXP: 246), 2)
        XCTAssertEqual(LevelSystem.level(forXP: 2000), 10)
    }

    func testProgressWithinLevel() {
        // à 246 XP : début du niveau 2 → progression 0 vers le niveau 3 (100×3^1.3 = 417)
        let p = LevelSystem.progress(forXP: 246)
        XCTAssertEqual(p.current, 0)
        XCTAssertEqual(p.needed, 417 - 246)
    }

    func testStepsCalorieEstimate() {
        // 8000 pas à 90 kg : 8000 × 0.04 × 90/70 ≈ 411
        XCTAssertEqual(StepsEstimator.kcal(steps: 8000, weightKg: 90), 411)
    }
}
```

- [ ] **Step 2 : Vérifier l'échec** — `swift test` — FAIL.

- [ ] **Step 3 : Implémenter**

```swift
// LevelSystem.swift
import Foundation

public enum LevelSystem {
    /// XP total requis pour ATTEINDRE le niveau n (niveau 1 = 0 XP). Seuil = 100 × n^1.3 arrondi (spec §7.1).
    public static func xpRequired(forLevel level: Int) -> Int {
        guard level > 1 else { return 0 }
        return Int((100 * pow(Double(level), 1.3)).rounded())
    }

    public static func level(forXP xp: Int) -> Int {
        var level = 1
        while xpRequired(forLevel: level + 1) <= xp { level += 1 }
        return level
    }

    /// Progression dans le niveau courant : XP déjà gagné / XP nécessaire jusqu'au prochain niveau.
    public static func progress(forXP xp: Int) -> (current: Int, needed: Int) {
        let level = level(forXP: xp)
        let base = xpRequired(forLevel: level)
        let next = xpRequired(forLevel: level + 1)
        return (xp - base, next - base)
    }
}
```

```swift
// StepsEstimator.swift
import Foundation

public enum StepsEstimator {
    /// Estimation indicative, jamais ajoutée au budget (spec §6) : pas × 0.04 × poids/70.
    public static func kcal(steps: Int, weightKg: Double) -> Int {
        Int((Double(steps) * 0.04 * weightKg / 70).rounded())
    }
}
```

- [ ] **Step 4 : Vérifier le succès** — `swift test` — PASS.
- [ ] **Step 5 : Commit** — `git commit -am "feat: level system (soft curve) and steps calorie estimator"`

---

## Task 5 : XPEngine (TDD)

**Files:** Create: `XPEngine.swift` — Test: `XPEngineTests.swift`

Règles (spec §7.1) : repas +20 (max 4 récompensés/jour), pesée +30 (max 1/jour), journée dans l'objectif +50, objectif de pas +40, quête +150, badge +50. L'XP ne descend jamais.

- [ ] **Step 1 : Tests**

```swift
import XCTest
@testable import NivelCore

final class XPEngineTests: XCTestCase {
    func testMealXPCappedAtFourPerDay() {
        XCTAssertEqual(XPEngine.award(.mealLogged, todayCount: 0), 20)
        XCTAssertEqual(XPEngine.award(.mealLogged, todayCount: 3), 20)
        XCTAssertEqual(XPEngine.award(.mealLogged, todayCount: 4), 0)
    }

    func testWeighInCappedAtOnePerDay() {
        XCTAssertEqual(XPEngine.award(.weighIn, todayCount: 0), 30)
        XCTAssertEqual(XPEngine.award(.weighIn, todayCount: 1), 0)
    }

    func testUncappedActions() {
        XCTAssertEqual(XPEngine.award(.dayWithinTarget, todayCount: 0), 50)
        XCTAssertEqual(XPEngine.award(.stepGoalReached, todayCount: 0), 40)
        XCTAssertEqual(XPEngine.award(.questCompleted, todayCount: 2), 150)
        XCTAssertEqual(XPEngine.award(.badgeUnlocked, todayCount: 5), 50)
    }
}
```

- [ ] **Step 2 : Échec** — `swift test` — FAIL.

- [ ] **Step 3 : Implémenter**

```swift
// XPEngine.swift
import Foundation

public enum XPAction: String, Codable, Sendable {
    case mealLogged, weighIn, dayWithinTarget, stepGoalReached, questCompleted, badgeUnlocked
}

public enum XPEngine {
    /// XP attribué pour une action, compte tenu du nombre de fois où elle a DÉJÀ été récompensée aujourd'hui.
    public static func award(_ action: XPAction, todayCount: Int) -> Int {
        switch action {
        case .mealLogged:      todayCount < 4 ? 20 : 0
        case .weighIn:         todayCount < 1 ? 30 : 0
        case .dayWithinTarget: 50
        case .stepGoalReached: 40
        case .questCompleted:  150
        case .badgeUnlocked:   50
        }
    }
}
```

- [ ] **Step 4 : Succès** — `swift test` — PASS.
- [ ] **Step 5 : Commit** — `git commit -am "feat: XP engine with daily caps"`

---

## Task 6 : Pool de quêtes + QuestEngine (TDD)

**Files:** Create: `QuestEngine.swift`, `Resources/quests.json` — Test: `QuestEngineTests.swift` (+ intégrité dans `CatalogsTests`)

- [ ] **Step 1 : Écrire `quests.json`** — 15 quêtes, formulations positives (spec §7.3). Champs : `id`, `title` (français), `emoji`, `metric`, `target`, `requiresSteps` (bool), `slot` (optionnel, filtre de créneau pour `mealsLogged`). Métriques : `mealsLogged`, `weeklySteps`, `weighIns`, `daysWithinTarget`, `daysWithoutAlcohol`, `lightDessertDays`, `stepGoalDays`.

```json
[
  {"id": "log_dinners_5",     "title": "Logge 5 dîners",                          "emoji": "🌙", "metric": "mealsLogged",      "target": 5,     "requiresSteps": false, "slot": "dinner"},
  {"id": "log_meals_10",      "title": "Logge 10 repas",                          "emoji": "📔", "metric": "mealsLogged",      "target": 10,    "requiresSteps": false},
  {"id": "log_meals_14",      "title": "Logge 14 repas",                          "emoji": "✍️", "metric": "mealsLogged",      "target": 14,    "requiresSteps": false},
  {"id": "steps_35k",         "title": "35 000 pas cette semaine",                "emoji": "👟", "metric": "weeklySteps",      "target": 35000, "requiresSteps": true},
  {"id": "steps_50k",         "title": "50 000 pas cette semaine",                "emoji": "🥾", "metric": "weeklySteps",      "target": 50000, "requiresSteps": true},
  {"id": "step_goal_3",       "title": "Atteins ton objectif de pas 3 jours",     "emoji": "🎯", "metric": "stepGoalDays",     "target": 3,     "requiresSteps": true},
  {"id": "weigh_in_1",        "title": "Pèse-toi une fois",                       "emoji": "⚖️", "metric": "weighIns",         "target": 1,     "requiresSteps": false},
  {"id": "within_target_3",   "title": "3 jours dans ton objectif kcal",          "emoji": "🍀", "metric": "daysWithinTarget", "target": 3,     "requiresSteps": false},
  {"id": "within_target_4",   "title": "4 jours dans ton objectif kcal",          "emoji": "🌟", "metric": "daysWithinTarget", "target": 4,     "requiresSteps": false},
  {"id": "no_alcohol_2",      "title": "2 jours sans alcool",                     "emoji": "💧", "metric": "daysWithoutAlcohol","target": 2,    "requiresSteps": false},
  {"id": "no_alcohol_4",      "title": "4 jours sans alcool",                     "emoji": "🫗", "metric": "daysWithoutAlcohol","target": 4,    "requiresSteps": false},
  {"id": "light_dessert_3",   "title": "3 jours en dessert léger (ou sans)",      "emoji": "🍎", "metric": "lightDessertDays", "target": 3,     "requiresSteps": false},
  {"id": "light_dessert_5",   "title": "5 jours en dessert léger (ou sans)",      "emoji": "🍏", "metric": "lightDessertDays", "target": 5,     "requiresSteps": false},
  {"id": "log_breakfast_4",   "title": "Logge 4 petits-déjeuners",                "emoji": "☀️", "metric": "mealsLogged",      "target": 4,     "requiresSteps": false, "slot": "breakfast"},
  {"id": "steps_25k",         "title": "25 000 pas cette semaine",                "emoji": "🚶", "metric": "weeklySteps",      "target": 25000, "requiresSteps": true}
]
```

Note : les quêtes de repas par créneau (`log_dinners_5`, `log_breakfast_4`) utilisent la métrique unique `mealsLogged` + champ optionnel `slot` — pas de métrique dédiée par créneau.

- [ ] **Step 2 : Tests (échec d'abord)** — points à couvrir :

```swift
import XCTest
@testable import NivelCore

final class QuestEngineTests: XCTestCase {
    func testDrawsThreeDistinctQuests() throws {
        let pool = try Catalogs.quests()
        let drawn = QuestEngine.weeklyDraw(pool: pool, weekID: "2026-W31", stepsAvailable: true)
        XCTAssertEqual(drawn.count, 3)
        XCTAssertEqual(Set(drawn.map(\.id)).count, 3)
    }

    func testDrawIsDeterministicForSameWeek() throws {
        let pool = try Catalogs.quests()
        let a = QuestEngine.weeklyDraw(pool: pool, weekID: "2026-W31", stepsAvailable: true)
        let b = QuestEngine.weeklyDraw(pool: pool, weekID: "2026-W31", stepsAvailable: true)
        XCTAssertEqual(a.map(\.id), b.map(\.id))
        let c = QuestEngine.weeklyDraw(pool: pool, weekID: "2026-W32", stepsAvailable: true)
        XCTAssertNotEqual(a.map(\.id), c.map(\.id)) // très probable ; pool de 15
    }

    func testExcludesStepQuestsWhenHealthKitDenied() throws {
        let pool = try Catalogs.quests()
        for week in 1...20 {
            let drawn = QuestEngine.weeklyDraw(pool: pool, weekID: "2026-W\(week)", stepsAvailable: false)
            XCTAssertTrue(drawn.allSatisfy { !$0.requiresSteps })
        }
    }

    func testWeekID() {
        // Mercredi 29 juillet 2026 → semaine ISO 31
        var cal = Calendar(identifier: .iso8601)
        cal.timeZone = TimeZone(identifier: "Europe/Paris")!
        let date = cal.date(from: DateComponents(year: 2026, month: 7, day: 29))!
        XCTAssertEqual(QuestEngine.weekID(for: date, calendar: cal), "2026-W31")
    }
}
```

- [ ] **Step 3 : Échec** — `swift test` — FAIL.

- [ ] **Step 4 : Implémenter** — points clés :

```swift
// QuestEngine.swift
import Foundation

public struct Quest: Codable, Identifiable, Hashable, Sendable {
    public enum Metric: String, Codable, Sendable {
        case mealsLogged, weeklySteps, weighIns,
             daysWithinTarget, daysWithoutAlcohol, lightDessertDays, stepGoalDays
    }
    public let id: String
    public let title: String
    public let emoji: String
    public let metric: Metric
    public let target: Int
    public let requiresSteps: Bool
    public let slot: MealSlot?
}

public enum QuestEngine {
    /// "AAAA-Wnn" — semaine ISO 8601 ; le renouvellement du lundi découle du changement de weekID (spec §7.3, §9).
    public static func weekID(for date: Date, calendar: Calendar) -> String {
        let week = calendar.component(.weekOfYear, from: date)
        let year = calendar.component(.yearForWeekOfYear, from: date)
        return "\(year)-W\(week)"
    }

    /// Tirage déterministe (seedé par weekID) de 3 quêtes du pool, sans HealthKit → sans quêtes de pas.
    public static func weeklyDraw(pool: [Quest], weekID: String, stepsAvailable: Bool) -> [Quest] {
        let eligible = pool.filter { stepsAvailable || !$0.requiresSteps }
        var generator = SeededGenerator(seed: fnv1a(weekID))
        return Array(eligible.shuffled(using: &generator).prefix(3))
    }

    /// Hash stable inter-lancements (String.hashValue ne l'est PAS).
    static func fnv1a(_ s: String) -> UInt64 {
        var hash: UInt64 = 0xcbf29ce484222325
        for byte in s.utf8 { hash = (hash ^ UInt64(byte)) &* 0x100000001b3 }
        return hash
    }
}

/// PRNG déterministe (SplitMix64) — hashValue de String n'est PAS stable entre lancements,
/// donc seed = FNV-1a du weekID, pas hashValue :
struct SeededGenerator: RandomNumberGenerator {
    var state: UInt64
    init(seed: UInt64) { state = seed }
    mutating func next() -> UInt64 {
        state &+= 0x9E3779B97F4A7C15
        var z = state
        z = (z ^ (z >> 30)) &* 0xBF58476D1CE4E5B9
        z = (z ^ (z >> 27)) &* 0x94D049BB133111EB
        return z ^ (z >> 31)
    }
}
```

⚠️ Le seed utilise `fnv1a(weekID)` (défini ci-dessus) et surtout PAS `String.hashValue`, qui change à chaque lancement du process et casserait la stabilité du tirage hebdomadaire.

La **progression** des quêtes (comptage `mealsLogged`, etc.) est calculée côté app (GameService, Task 14) à partir des données SwiftData de la semaine : QuestEngine expose seulement `weeklyDraw` et `weekID`.

- [ ] **Step 5 : Succès** — `swift test` — PASS. Ajouter au `CatalogsTests` : `quests()` charge 15 entrées, ids uniques.
- [ ] **Step 6 : Commit** — `git commit -am "feat: weekly quest pool and deterministic quest draw"`

---

## Task 7 : Badges + BadgeEngine (TDD)

**Files:** Create: `BadgeEngine.swift`, `Resources/badges.json` — Test: `BadgeEngineTests.swift`

- [ ] **Step 1 : `badges.json`** — 20 badges (spec §7.2). Champs : `id`, `title`, `emoji`, `hint` (indice affiché quand verrouillé), `metric`, `threshold`. Métriques : `weighIns`, `mealsLogged`, `journalDays`, `stepsInOneDay`, `totalSteps`, `totalKm`, `level`, `questsCompleted`, `weekWithinTarget`, `trendDownFortnight`.

```json
[
  {"id": "first_weigh",     "title": "Première pesée",            "emoji": "⚖️", "hint": "Pèse-toi une première fois",            "metric": "weighIns",          "threshold": 1},
  {"id": "first_meal",      "title": "Premier repas",             "emoji": "🍽️", "hint": "Logge ton premier repas",               "metric": "mealsLogged",       "threshold": 1},
  {"id": "journal_7",       "title": "Une semaine de journal",    "emoji": "📅", "hint": "7 jours avec au moins un repas loggé",  "metric": "journalDays",       "threshold": 7},
  {"id": "journal_30",      "title": "Un mois de journal",        "emoji": "🗓️", "hint": "30 jours avec au moins un repas loggé", "metric": "journalDays",       "threshold": 30},
  {"id": "steps_10k",       "title": "10 000 pas !",              "emoji": "👟", "hint": "10 000 pas en une journée",             "metric": "stepsInOneDay",     "threshold": 10000},
  {"id": "steps_15k",       "title": "Grand marcheur",            "emoji": "🥾", "hint": "15 000 pas en une journée",             "metric": "stepsInOneDay",     "threshold": 15000},
  {"id": "steps_total_100k","title": "100 000 pas cumulés",       "emoji": "🚶", "hint": "Cumule 100 000 pas",                    "metric": "totalSteps",        "threshold": 100000},
  {"id": "km_100",          "title": "100 km au compteur",        "emoji": "🗺️", "hint": "Cumule 100 km de marche",               "metric": "totalKm",           "threshold": 100},
  {"id": "level_5",         "title": "Niveau 5",                  "emoji": "⭐", "hint": "Atteins le niveau 5",                   "metric": "level",             "threshold": 5},
  {"id": "level_10",        "title": "Niveau 10",                 "emoji": "🌟", "hint": "Atteins le niveau 10",                  "metric": "level",             "threshold": 10},
  {"id": "level_20",        "title": "Niveau 20",                 "emoji": "💫", "hint": "Atteins le niveau 20",                  "metric": "level",             "threshold": 20},
  {"id": "quest_first",     "title": "Première quête",            "emoji": "🎯", "hint": "Complète une quête hebdo",              "metric": "questsCompleted",   "threshold": 1},
  {"id": "quest_10",        "title": "Aventurier confirmé",       "emoji": "🏹", "hint": "Complète 10 quêtes hebdo",              "metric": "questsCompleted",   "threshold": 10},
  {"id": "week_target",     "title": "Semaine sereine",           "emoji": "🍀", "hint": "7 jours d'affilée dans ton objectif",   "metric": "weekWithinTarget",  "threshold": 1},
  {"id": "trend_down",      "title": "La tendance est bonne",     "emoji": "📉", "hint": "Tendance de poids en baisse sur 2 semaines", "metric": "trendDownFortnight", "threshold": 1},
  {"id": "weigh_10",        "title": "10 pesées",                 "emoji": "🧭", "hint": "Pèse-toi 10 fois",                      "metric": "weighIns",          "threshold": 10},
  {"id": "meals_50",        "title": "50 repas loggés",           "emoji": "📖", "hint": "Logge 50 repas",                        "metric": "mealsLogged",       "threshold": 50},
  {"id": "meals_200",       "title": "200 repas loggés",          "emoji": "📚", "hint": "Logge 200 repas",                       "metric": "mealsLogged",       "threshold": 200},
  {"id": "steps_total_1m",  "title": "Un million de pas",         "emoji": "🏆", "hint": "Cumule 1 000 000 de pas",               "metric": "totalSteps",        "threshold": 1000000},
  {"id": "journal_100",     "title": "100 jours de journal",      "emoji": "🎖️", "hint": "100 jours avec au moins un repas loggé","metric": "journalDays",       "threshold": 100}
]
```

- [ ] **Step 2 : Tests (échec d'abord)**

```swift
import XCTest
@testable import NivelCore

final class BadgeEngineTests: XCTestCase {
    func testUnlocksOnlyNewBadgesAtOrAboveThreshold() throws {
        let badges = try Catalogs.badges()
        var stats = BadgeStats()
        stats.weighIns = 1
        stats.level = 5
        let unlocked = BadgeEngine.newlyUnlocked(badges: badges, stats: stats, alreadyUnlocked: ["level_5"])
        XCTAssertEqual(Set(unlocked.map(\.id)), ["first_weigh"])   // level_5 déjà eu, seuils non atteints ailleurs
    }

    func testNoUnlockBelowThreshold() throws {
        let badges = try Catalogs.badges()
        let unlocked = BadgeEngine.newlyUnlocked(badges: badges, stats: BadgeStats(), alreadyUnlocked: [])
        XCTAssertTrue(unlocked.isEmpty)
    }
}
```

- [ ] **Step 3 : Échec** — FAIL. — **Step 4 : Implémenter**

```swift
// BadgeEngine.swift
import Foundation

public struct Badge: Codable, Identifiable, Hashable, Sendable {
    public enum Metric: String, Codable, Sendable {
        case weighIns, mealsLogged, journalDays, stepsInOneDay, totalSteps,
             totalKm, level, questsCompleted, weekWithinTarget, trendDownFortnight
    }
    public let id: String
    public let title: String
    public let emoji: String
    public let hint: String
    public let metric: Metric
    public let threshold: Int
}

/// Snapshot des compteurs de l'utilisateur — rempli par GameService côté app.
public struct BadgeStats: Sendable {
    public var weighIns = 0, mealsLogged = 0, journalDays = 0, stepsInOneDay = 0,
               totalSteps = 0, totalKm = 0, level = 1, questsCompleted = 0,
               weekWithinTarget = 0, trendDownFortnight = 0
    public init() {}

    func value(for metric: Badge.Metric) -> Int {
        switch metric {
        case .weighIns: weighIns
        case .mealsLogged: mealsLogged
        case .journalDays: journalDays
        case .stepsInOneDay: stepsInOneDay
        case .totalSteps: totalSteps
        case .totalKm: totalKm
        case .level: level
        case .questsCompleted: questsCompleted
        case .weekWithinTarget: weekWithinTarget
        case .trendDownFortnight: trendDownFortnight
        }
    }
}

public enum BadgeEngine {
    public static func newlyUnlocked(badges: [Badge], stats: BadgeStats, alreadyUnlocked: Set<String>) -> [Badge] {
        badges.filter { !alreadyUnlocked.contains($0.id) && stats.value(for: $0.metric) >= $0.threshold }
    }
}
```

Ajouter `Catalogs.badges()` et `Catalogs.quests()` dans `Catalogs.swift`, + test d'intégrité (20 badges, ids uniques).

- [ ] **Step 5 : Succès** — `swift test` — PASS. — **Step 6 : Commit** — `git commit -am "feat: badge catalog and unlock engine"`

---

## Task 8 : Banque de messages + MessageBank (TDD)

**Files:** Create: `MessageBank.swift`, `Resources/messages.json` — Test: `MessageBankTests.swift`

- [ ] **Step 1 : Écrire `messages.json`** — structure : `{"context": [ {"id": "...", "text": "..."} ]}`. Contextes : `morning`, `midday`, `evening`, `afterMealLog`, `afterWeighIn`, `weighReminder` (invitations à se peser, PAS des félicitations), `levelUp`, `badge`, `questCompleted`, `stepsEncouragement`, `overTarget`, `comeback`, `fun`. **≥ 12 messages par contexte (≥ 156 au total).** Le texte peut contenir `{name}` (prénom) et `{value}` (nombre contextuel). Ton : chaleureux, drôle, jamais culpabilisant ; tutoiement ; emojis avec parcimonie. Exemples de calibration (à étoffer dans le même esprit) :

```json
{
  "morning":   [{"id": "m1", "text": "Bonjour {name} ! Prêt·e pour une belle journée ? ☀️"},
                {"id": "m2", "text": "Salut {name} ! Un petit-déj et c'est parti."}],
  "afterMealLog": [{"id": "aml1", "text": "Miam, c'est noté ! +{value} XP 🎉"},
                   {"id": "aml2", "text": "Journal à jour, chef·fe !"}],
  "overTarget": [{"id": "ot1", "text": "Ça arrive à tout le monde 😌 Demain est un autre jour."},
                 {"id": "ot2", "text": "Un bon repas, ça fait partie de la vie. On continue tranquillement."}],
  "comeback":  [{"id": "cb1", "text": "Content de te revoir {name} ! Tu m'as manqué 🧡"},
                {"id": "cb2", "text": "Re-bienvenue ! On reprend en douceur, comme toujours."}]
}
```

⚠️ Le contexte `overTarget` ne doit JAMAIS contenir de reproche, de chiffre de dépassement, ni de "mais". Le contexte `comeback` ne mentionne jamais la durée d'absence.

- [ ] **Step 2 : Tests (échec d'abord)**

```swift
import XCTest
@testable import NivelCore

final class MessageBankTests: XCTestCase {
    func testBankHasAllContextsWithEnoughVariety() throws {
        let bank = try MessageBank.load()
        for context in MessageContext.allCases {
            XCTAssertGreaterThanOrEqual(bank.messages(for: context).count, 12, "\(context) trop pauvre")
        }
    }

    func testPickAvoidsImmediateRepetition() throws {
        let bank = try MessageBank.load()
        var lastID: String? = nil
        for _ in 0..<50 {
            let msg = bank.pick(context: .morning, excluding: lastID, name: "Michaël", value: nil)
            XCTAssertNotEqual(msg.id, lastID)
            lastID = msg.id
        }
    }

    func testPlaceholdersAreSubstituted() throws {
        let bank = try MessageBank.load()
        let msg = bank.pick(context: .afterMealLog, excluding: nil, name: "Marion", value: 20)
        XCTAssertFalse(msg.text.contains("{name}"))
        XCTAssertFalse(msg.text.contains("{value}"))
    }
}
```

- [ ] **Step 3 : Échec** — FAIL. — **Step 4 : Implémenter**

```swift
// MessageBank.swift
import Foundation

public enum MessageContext: String, Codable, CaseIterable, Sendable {
    case morning, midday, evening, afterMealLog, afterWeighIn, weighReminder,
         levelUp, badge, questCompleted, stepsEncouragement, overTarget, comeback, fun
}

public struct NivelitoMessage: Codable, Identifiable, Sendable {
    public let id: String
    public let text: String
}

public struct MessageBank: Sendable {
    private let store: [MessageContext: [NivelitoMessage]]

    public static func load() throws -> MessageBank {
        let raw: [String: [NivelitoMessage]] = try Catalogs.load("messages")
        var store: [MessageContext: [NivelitoMessage]] = [:]
        for (key, msgs) in raw {
            guard let ctx = MessageContext(rawValue: key) else { continue }
            store[ctx] = msgs
        }
        return MessageBank(store: store)
    }

    public func messages(for context: MessageContext) -> [NivelitoMessage] { store[context] ?? [] }

    /// Choix aléatoire hors dernier message utilisé, avec substitution {name}/{value} (spec §8).
    public func pick(context: MessageContext, excluding lastID: String?, name: String, value: Int?) -> NivelitoMessage {
        let candidates = messages(for: context).filter { $0.id != lastID }
        let chosen = candidates.randomElement() ?? NivelitoMessage(id: "fallback", text: "Salut {name} !")
        var text = chosen.text.replacingOccurrences(of: "{name}", with: name)
        if let value { text = text.replacingOccurrences(of: "{value}", with: String(value)) }
        return NivelitoMessage(id: chosen.id, text: text)
    }
}
```

- [ ] **Step 5 : Succès** — `swift test` — PASS (la rédaction des ~150 messages fait partie de ce step ; c'est du contenu, pas du code — soigner la variété et le ton).
- [ ] **Step 6 : Commit** — `git commit -am "feat: Nivelito message bank with contextual non-repeating picks"`

---

## Task 9 : Projet Xcode (XcodeGen) + Theme + modèles SwiftData

**Files:** Create: `project.yml`, `App/NivelApp.swift`, `App/Theme.swift`, `App/RootView.swift`, `App/Models/PersistentModels.swift`, `App/Assets.xcassets` (vide pour l'instant), `NivelTests/AppSmokeTests.swift`

- [ ] **Step 1 : `project.yml`**

```yaml
name: Nivel
options:
  bundleIdPrefix: fr.mbernard
  deploymentTarget:
    iOS: "17.0"
packages:
  NivelCore:
    path: NivelCore
targets:
  Nivel:
    type: application
    platform: iOS
    sources: [App]
    dependencies:
      - package: NivelCore
    entitlements:
      path: App/Nivel.entitlements
      properties:
        com.apple.developer.healthkit: true
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: Nivel
        CFBundleDevelopmentRegion: fr
        NSHealthShareUsageDescription: "Nivel lit tes pas pour suivre ton activité quotidienne."
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
  NivelTests:
    type: bundle.unit-test
    platform: iOS
    sources: [NivelTests]
    dependencies:
      - target: Nivel
schemes:
  Nivel:
    build: { targets: { Nivel: all, NivelTests: [test] } }
    test: { targets: [NivelTests] }
```

- [ ] **Step 2 : Theme**

```swift
// App/Theme.swift
import SwiftUI

enum Theme {
    static let background = Color(hex: 0xFDF6EC)
    static let card = Color.white
    static let text = Color(hex: 0x5B4A3F)
    static let subtext = Color(hex: 0xB09A8A)
    static let orange = Color(hex: 0xF57C1F)
    static let accent = Color(hex: 0xF5B453)
    static let green = Color(hex: 0x7BC86C)
    static let blue = Color(hex: 0x4DA3C7)
    static let track = Color(hex: 0xF4E7DB)
    static let outline = Color(hex: 0x3A1220)

    static let cardRadius: CGFloat = 24
    static let buttonRadius: CGFloat = 22
}

extension Color {
    init(hex: UInt32) {
        self.init(red: Double((hex >> 16) & 0xFF) / 255,
                  green: Double((hex >> 8) & 0xFF) / 255,
                  blue: Double(hex & 0xFF) / 255)
    }
}

struct CardStyle: ViewModifier {
    func body(content: Content) -> some View {
        content
            .padding(14)
            .background(Theme.card, in: RoundedRectangle(cornerRadius: Theme.cardRadius))
            .shadow(color: .black.opacity(0.06), radius: 8, y: 4)
    }
}
extension View { func card() -> some View { modifier(CardStyle()) } }
```

Toute l'app utilise `.fontDesign(.rounded)` (à poser sur RootView).

- [ ] **Step 3 : Modèles SwiftData** — miroir de la spec §9 :

```swift
// App/Models/PersistentModels.swift
import Foundation
import SwiftData
import NivelCore

@Model final class UserProfile {
    var name: String
    var sexRaw: String
    var birthDate: Date
    var heightCm: Double
    var initialWeightKg: Double
    var activityRaw: String
    var dailyCalorieTarget: Int          // calculé à l'onboarding, modifiable (spec §6)
    var dailyStepGoal: Int               // défaut 8000
    var remindersEnabled: [String: Bool] // id de rappel → actif
    var createdAt: Date
    var lastMessageIDs: [String: String] // contexte → dernier id de message (anti-répétition)
    // init avec valeurs par défaut ; computed vars sex/activity typées NivelCore
}

@Model final class MealEntry {
    var date: Date
    var slotRaw: String
    var dishID: String
    var portionRaw: String
    var extras: [String: Int]            // extraID → quantité
    var estimatedKcal: Int
    var xpAwarded: Int
}

@Model final class WeightEntry {
    var date: Date
    var weightKg: Double
}

@Model final class DayLog {
    @Attribute(.unique) var day: Date    // minuit local
    var steps: Int
    var kcalEaten: Int
    var kcalTarget: Int
    var xpEarned: Int
    var withinTarget: Bool
    var closed: Bool                     // clôturé par DayCloser
}

@Model final class GamificationState {
    var totalXP: Int
    var badgeUnlocks: [String: Date]     // badgeID → date
    var activeQuestIDs: [String]
    var questWeekID: String
    var questProgress: [String: Int]
    var completedQuestIDs: [String]      // historique (pour badges)
    var lastClosedDay: Date?
}
```

- [ ] **Step 4 : App + Root**

```swift
// App/NivelApp.swift
import SwiftUI
import SwiftData

@main
struct NivelApp: App {
    let container: ModelContainer = {
        try! ModelContainer(for: UserProfile.self, MealEntry.self, WeightEntry.self,
                            DayLog.self, GamificationState.self)
    }()

    var body: some Scene {
        WindowGroup {
            RootView()
                .fontDesign(.rounded)
                .modelContainer(container)
        }
    }
}
```

`RootView` : si aucun `UserProfile` en base → `OnboardingFlow` (placeholder `Text("Onboarding")` pour l'instant), sinon `TabView` à 5 onglets avec placeholders (`Text("Accueil")`, etc.) sur fond `Theme.background`. Le splash sera ajouté en Task 19.

- [ ] **Step 5 : Smoke test** — `NivelTests/AppSmokeTests.swift` : un test qui crée le `ModelContainer` en mémoire (`isStoredInMemoryOnly: true`) et insère un `UserProfile` + un `MealEntry`.

- [ ] **Step 6 : Générer et builder**

Run: `xcodegen generate && xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`
Expected: `BUILD SUCCEEDED`. Puis les tests : `xcodebuild … test` → tests app PASS.
Ajouter `Nivel.xcodeproj` à `.gitignore`? **Non** — le committer simplifie la vie de Michaël ; mais il est régénérable. Décision : committer `project.yml` ET le `.xcodeproj` généré.

- [ ] **Step 7 : Commit** — `git add -A && git commit -m "feat: Xcode project (XcodeGen), theme, SwiftData models, app skeleton"`

---

## Task 10 : Nivelito — formes, expressions, animations

**Files:** Create: `App/Nivelito/NivelitoShapes.swift`, `App/Nivelito/NivelitoView.swift`, `App/Nivelito/SpeechBubble.swift`

Traduction fidèle de `design/nivelito.svg` (espace 200×200) en `Path` SwiftUI. Conversion mécanique : SVG `C x1 y1, x2 y2, x y` → `path.addCurve(to: CGPoint(x, y), control1: CGPoint(x1, y1), control2: CGPoint(x2, y2))` ; `Q cx cy, x y` → `addQuadCurve`. Encapsuler dans un `GeometryReader`/`scaleEffect` pour rendre la taille paramétrable (l'espace de référence reste 200×200 via `path.applying(CGAffineTransform(scaleX: size/200, y: size/200))`).

- [ ] **Step 1 : NivelitoShapes.swift** — un `Shape` par élément, points copiés du SVG :

```swift
import SwiftUI

/// Tête (contour extérieur) — d'après design/nivelito.svg
struct NivelitoHead: Shape {
    func path(in rect: CGRect) -> Path {
        var p = Path()
        p.move(to: .init(x: 100, y: 44))
        p.addCurve(to: .init(x: 176, y: 104), control1: .init(x: 146, y: 44), control2: .init(x: 173, y: 64))
        p.addCurve(to: .init(x: 100, y: 168), control1: .init(x: 179, y: 142), control2: .init(x: 148, y: 168))
        p.addCurve(to: .init(x: 24, y: 104), control1: .init(x: 52, y: 168), control2: .init(x: 21, y: 142))
        p.addCurve(to: .init(x: 100, y: 44), control1: .init(x: 27, y: 64), control2: .init(x: 54, y: 44))
        p.closeSubpath()
        return p.scaled(toFit: rect, reference: 200)
    }
}
// Idem : NivelitoEarLeft (M36 76 C26 48 34 26 56 24 C70 30 82 42 87 56 Z),
// NivelitoEarRight (miroir), NivelitoEarInnerLeft/Right, NivelitoCheekPatch (ellipses),
// NivelitoMuzzle (M100 94 C128 94 148 108 148 130 C148 152 127 166 100 166 C73 166 52 152 52 130 C52 108 72 94 100 94 Z),
// NivelitoBrowLeft/Right (virgules), NivelitoNose (M90 122 Q100 116 110 122 Q108 134 100 136 Q92 134 90 122 Z),
// NivelitoMouth (philtrum + double quad curve, en stroke).

extension Path {
    /// Met à l'échelle un path défini dans un espace de référence carré (200×200) vers rect.
    func scaled(toFit rect: CGRect, reference: CGFloat) -> Path {
        let s = min(rect.width, rect.height) / reference
        return applying(CGAffineTransform(scaleX: s, y: s))
    }
}
```

Reprendre **tous** les tracés du SVG (oreilles + intérieurs, tête, sourcils, joues brunes, museau, truffe, bouche, blush). Contours : `.stroke(Theme.outline, style: StrokeStyle(lineWidth: 9 * scale, lineJoin: .round))` par-dessus le fill, comme le SVG.

- [ ] **Step 2 : NivelitoView** — expressions + animations :

```swift
enum NivelitoExpression { case happy, joy, wink, sleepy, encouraging }

struct NivelitoView: View {
    var expression: NivelitoExpression = .happy
    var size: CGFloat = 100
    @State private var isBlinking = false
    @State private var breathe = false
    // Yeux : cercles (happy/encouraging), arcs fermés joyeux (joy), un seul œil fermé (wink),
    //        paupières lourdes = arcs plats (sleepy). Bouche : sourire (défaut), ouverte (joy).
    // Idle : scaleEffect(y: breathe ? 1.02 : 0.98) animé .easeInOut(duration: 2).repeatForever
    //        + clignement : réduire la hauteur des yeux 0.12 s toutes les 3–6 s (Timer aléatoire).
    // Joy : rotation légère ±4° + offset y −8 en spring, à déclencher via .celebrate trigger externe.
}
```

`SpeechBubble` : fond blanc, radius 18 (4 en bas-gauche), ombre douce, texte `Theme.text`, max 2-3 lignes — reprend la maquette.

- [ ] **Step 3 : Prévisualiser** — `#Preview` avec les 5 expressions côte à côte. Build : `xcodebuild … build` → SUCCEEDED. Vérification visuelle dans le canvas Xcode (ou lancer l'app placeholder avec un NivelitoView plein écran) : la tête doit être fidèle au SVG.

- [ ] **Step 4 : Commit** — `git commit -am "feat: Nivelito SwiftUI shapes, expressions and idle animations"`

---

## Task 11 : Services — HealthKit + GameService

**Files:** Create: `App/Services/HealthKitService.swift`, `App/Services/GameService.swift`

- [ ] **Step 1 : HealthKitService**

```swift
import Foundation
import HealthKit

/// Abstraction testable (spec §11)
protocol StepsProviding {
    var isAvailable: Bool { get }
    func requestAuthorization() async -> Bool
    func steps(on day: Date) async -> Int?          // nil si refusé/indispo
    func dailySteps(from: Date, to: Date) async -> [Date: Int]
}

final class HealthKitService: StepsProviding { /* HKStatisticsCollectionQuery quotidienne, .cumulativeSum sur stepCount */ }
final class FakeStepsService: StepsProviding { /* dictionnaire injectable — pour previews et tests */ }
```

- [ ] **Step 2 : GameService** — façade `@Observable` unique consommée par les vues. Responsabilités :
  - `logMeal(...)` : crée `MealEntry` (kcal via `MealEstimator`), attribue l'XP (`XPEngine.award(.mealLogged, todayCount:)` — todayCount = nb de repas déjà récompensés aujourd'hui), met à jour `DayLog` du jour, recalcule la progression des quêtes, évalue les badges (`BadgeEngine`), détecte le level-up (`LevelSystem.level` avant/après) et publie les événements UI (`pendingCelebration: Celebration?` — levelUp/badge/quest).
  - `logWeight(...)` : idem pour la pesée.
  - `refreshQuestProgress()` : calcule chaque métrique de quête depuis SwiftData + pas de la semaine (`weeklySteps` via StepsProviding), marque les quêtes complétées (+150 XP, une seule fois).
  - `badgeStats()` : construit `BadgeStats` depuis SwiftData (dont `totalKm` = totalSteps × 0.00075, `trendDownFortnight` via la pente de la tendance lissée de Task 15).
  - `nivelitoSays(context:)` : `MessageBank.pick` + persiste `lastMessageIDs`.
  Injecter `StepsProviding` et `ModelContext` à l'init.

- [ ] **Step 3 : Build + test rapide** — build OK ; ajouter dans `NivelTests` un test de `GameService.logMeal` avec container en mémoire + `FakeStepsService` : vérifie kcal, XP plafonné, DayLog mis à jour.

- [ ] **Step 4 : Commit** — `git commit -am "feat: HealthKit abstraction and GameService facade"`

---

## Task 12 : Onboarding

**Files:** Create: `App/Views/Onboarding/OnboardingFlow.swift` — Modify: `App/RootView.swift`

Flux (spec §5) en pages avec transitions spring, Nivelito présent partout :
1. Bienvenue — Nivelito (joy) se présente : "Salut ! Moi c'est Nivelito 🧡 …".
2. Choix du profil — deux grosses cartes "Michaël" / "Marion" (détermine `name` + `sex` : Michaël → male, Marion → female ; le sexe reste modifiable à l'étape suivante).
3. Infos — taille (cm, Stepper/roue), date de naissance (DatePicker), poids actuel (kg, décimal), niveau d'activité (4 cartes avec description française : "Sédentaire — surtout assis", "Léger — je bouge un peu chaque jour", "Modéré — actif régulièrement", "Actif — sport fréquent").
4. Objectif — Nivelito annonce le résultat de `CalorieCalculator.dailyTarget` : "Je te propose **{X} kcal par jour** — une perte douce, sans pression. Tu pourras l'ajuster quand tu veux." Bouton "C'est parti !".
5. Autorisations — deux cartes : Santé (pas) → `requestAuthorization()`, Notifications → `UNUserNotificationCenter.requestAuthorization`. Refus OK (spec §5) : l'app continue.

À la fin : créer `UserProfile` + `WeightEntry` initiale + `GamificationState` (XP 0, quêtes tirées pour la semaine courante), programmer les notifications (Task 17 — appeler un stub pour l'instant), basculer sur les tabs.

- [ ] **Step 1 : Implémenter le flux** (un fichier, sous-vues privées par page).
- [ ] **Step 2 : Vérifier** — build + lancement simulateur : parcours complet, profil persisté (relance → arrive sur les tabs), valeurs cohérentes.
- [ ] **Step 3 : Commit** — `git commit -am "feat: onboarding flow with profile creation and permissions"`

---

## Task 13 : Accueil (dashboard)

**Files:** Create: `App/Views/Home/HomeView.swift` (+ sous-vues `CalorieRing`, `StatCard`, `XPBar` dans le même fichier ou fichiers séparés si > ~300 lignes) — Modify: `App/RootView.swift` (brancher l'onglet)

Reproduire la maquette validée (`design-home.html`) :
- En-tête : date française (`Date.FormatStyle` `.weekday(.wide).day().month(.wide)`), "Salut {prénom} 👋", pastille NIVEAU n.
- Nivelito (size 86) + `SpeechBubble` avec message contextuel : priorité aux événements du jour (level-up récent, badge) sinon salutation horaire (`morning`/`midday`/`evening`), calculé via `GameService.nivelitoSays`.
- `CalorieRing` : anneau `Circle().trim(to: progress)` stroke 12, vert `Theme.green`, piste `Theme.track`, valeur au centre (`mangé / objectif`), sous-titre "Reste X kcal 🍽️" — si dépassement : anneau passe à `Theme.accent` (jamais rouge) et sous-titre neutre "Objectif dépassé de X — ça arrive 😌".
- `StatCard` pas : compteur du jour (StepsProviding), jauge vers `dailyStepGoal`, "obj. 8 000". **Si l'autorisation HealthKit est refusée/indisponible (`steps(on:)` renvoie nil), la carte de pas est masquée** (spec §10) — la carte XP prend toute la hauteur.
- `XPBar` : progression `LevelSystem.progress(forXP:)`, libellé "650 / 1 000 → niv. 8".
- Carte quête la plus avancée (non complétée) avec jauge.
- Bouton "+ Logger un repas" (dégradé accent→orange, radius 22) → ouvre `MealLogSheet` (Task 14 ; stub pour l'instant).
- Pull-to-refresh : re-lit les pas.

- [ ] **Step 1 : Implémenter** avec previews alimentées par `FakeStepsService` + container en mémoire.
- [ ] **Step 2 : Vérifier** — build + simulateur : affichage correct avec profil créé, pas de rouge nulle part.
- [ ] **Step 3 : Commit** — `git commit -am "feat: home dashboard (calorie ring, steps, XP, quest, Nivelito)"`

---

## Task 14 : Log de repas + journal

**Files:** Create: `App/Views/Meals/MealLogSheet.swift`, `App/Views/Meals/MealsJournalView.swift` — Modify: `HomeView.swift` (brancher la sheet), `RootView.swift` (onglet Repas)

**MealLogSheet** (spec §4.2) — une seule sheet, 3 étapes visibles simultanément en scroll (pas un wizard) pour tenir les "moins de 15 secondes" :
- En tête : sélecteur de créneau pré-rempli (`MealSlot.suggested(forHour:)`), 4 chips.
- Section plat : grille 2 colonnes de cartes (emoji + nom + ~kcal), les plats du créneau courant d'abord (`slots`), les autres derrière un "Tout afficher". Sélection unique.
- Section portion : 3 segments Léger / Normal / Copieux (défaut Normal).
- Section extras : chips multi-sélection avec stepper de quantité pour les boissons (bière/vin/soda ; eau sans quantité), dessert léger/gourmand exclusifs.
- Bandeau bas collant : "≈ {kcal} kcal" mis à jour en direct (`MealEstimator.estimate`) + bouton "Valider (+20 XP)". Validation → `GameService.logMeal` → dismiss + toast/bulle Nivelito `afterMealLog` sur l'accueil + éventuelle célébration.

**MealsJournalView** : journal du jour groupé par créneau (sections Petit-déj / Déjeuner / Dîner / Encas), chaque entrée swipe-to-delete et tap-to-edit (rouvre la sheet pré-remplie) **si l'entrée date d'aujourd'hui** (spec : modifiable le jour même). Navigation jours précédents : chevrons ← → en lecture seule. Total du jour vs objectif en tête (neutre si dépassé).

- [ ] **Step 1 : MealLogSheet** + vérif simulateur (log en < 15 s, kcal exactes vs test connu : pâtes copieux + 2 bières + dessert gourmand = 1445).
- [ ] **Step 2 : MealsJournalView** + vérif (édition jour même seulement, suppression met à jour DayLog).
- [ ] **Step 3 : Commit** — `git commit -am "feat: 3-step meal logging sheet and daily journal"`

---

## Task 15 : Pesée + écran Progrès (courbes)

**Files:** Create: `App/Views/Progress/ProgressView.swift` (+ `WeighInSheet`, `WeightChart`, `CaloriesChart`, `StepsChart`) — Modify: `RootView.swift`

Swift Charts (spec §4.3) :
- **WeightChart** : `PointMark` pesées (subtext, petites) + `LineMark` tendance lissée mise en avant (orange, épaisse). Tendance = moyenne mobile exponentielle : `trend[i] = trend[i-1] + 0.25 × (poids[i] − trend[i-1])` (implémenter `WeightTrend.smooth(_:)` dans **NivelCore** + test unitaire : la tendance de [80, 82, 79] est monotone décroissante après lissage… vérifier plutôt : valeurs exactes 80, 80.5, 80.125). Sélecteur de période : 1 m / 3 m / tout.
- **CaloriesChart** : `BarMark` par jour (mangé), `RuleMark` objectif ; barres au-dessus de l'objectif en `Theme.accent` (PAS rouge). Semaine / mois.
- **StepsChart** : `BarMark` pas/jour (bleu), record personnel annoté 🏆. Semaine / mois. **Masqué entièrement (section absente) si l'autorisation HealthKit est refusée** (spec §10).
- Bouton "+ Pesée" → `WeighInSheet` : saisie décimale kg, date = aujourd'hui, bouton Valider (+30 XP) → `GameService.logWeight`, bulle Nivelito `afterWeighIn`.

- [ ] **Step 1 : `WeightTrend` dans NivelCore (TDD** : test des valeurs exactes ci-dessus**)** — `swift test` PASS.
- [ ] **Step 2 : Les 3 charts + sheet**, previews avec données fictives ; vérif simulateur.
- [ ] **Step 3 : Commit** — `git commit -am "feat: progress screen with weight trend, calories and steps charts"`

---

## Task 16 : Écran Quêtes & badges

**Files:** Create: `App/Views/Quests/QuestsView.swift` — Modify: `RootView.swift`

- Section "Quêtes de la semaine" : 3 cartes (emoji, titre, jauge de progression, "x/y", ✓ + or si complétée). Sous-titre discret : "Nouvelles quêtes lundi 🌱".
- Section "Trophées" : grille 3 colonnes ; débloqué = emoji plein + titre + date, verrouillé = pastille grise + `hint` en petit. Compteur "x / 20".
- Tap sur badge débloqué → petite fiche (sheet medium) avec date et phrase de Nivelito.

- [ ] **Step 1 : Implémenter + vérif simulateur** (débloquer un badge de test en loggant un repas → il apparaît).
- [ ] **Step 2 : Commit** — `git commit -am "feat: quests and trophies screen"`

---

## Task 17 : Réglages + notifications

**Files:** Create: `App/Views/Settings/SettingsView.swift`, `App/Services/NotificationService.swift` — Modify: `OnboardingFlow.swift` (appeler la vraie planification)

**NotificationService** (spec §10) — 4 rappels identifiés, `UNCalendarNotificationTrigger` répétitifs :

| id | déclencheur | contexte messages |
|----|-------------|-------------------|
| `lunch` | tous les jours 12 h 30 | `midday` |
| `dinner` | tous les jours 20 h 00 | `evening` |
| `weigh` | samedi 9 h 00 | `weighReminder` (invitations, jamais de félicitations anticipées) |
| `steps` | tous les jours 18 h 00 | `stepsEncouragement` |

`reschedule(for profile:)` : supprime tout (`removeAllPendingNotificationRequests`) puis re-planifie chaque rappel actif avec un texte tiré de la banque (varie à chaque re-planification ; re-planifier à chaque passage au premier plan pour renouveler les textes). Titre : "Nivelito 🧡".

**SettingsView** (spec §4.5) : sections Profil (prénom en lecture, taille/date de naissance/sexe/activité éditables → bouton "Recalculer mon objectif" qui affiche l'ancien → nouveau), Objectifs (kcal en saisie directe avec note "calculé : X", pas quotidien), Rappels (4 toggles), Santé (état de l'autorisation + lien Réglages système si refusée), À propos (version, "Fait avec 🧡 pour Michaël & Marion").

- [ ] **Step 1 : NotificationService + branchement onboarding.**
- [ ] **Step 2 : SettingsView** ; vérif : toggles persistés, notification test reçue (planifier à +1 min en debug).
- [ ] **Step 3 : Commit** — `git commit -am "feat: settings screen and gentle local reminders"`

---

## Task 18 : DayCloser (clôture des journées passées)

**Files:** Create: `App/Services/DayCloser.swift` — Modify: `App/NivelApp.swift` (scenePhase), `NivelTests/DayCloserTests.swift`

Spec §9 (traitements différés) : au passage au premier plan (`scenePhase == .active`) :
1. Pour chaque jour depuis `lastClosedDay` (exclu) jusqu'à hier (inclus) : lire les pas de l'historique HealthKit, figer `DayLog` (steps, kcalEaten depuis les MealEntry, withinTarget = `kcalEaten ≤ kcalTarget` **ET** ≥ 2 repas loggés — sans log, pas de jugement), attribuer +50 (`dayWithinTarget`) et +40 (`stepGoalReached` si steps ≥ objectif), marquer `closed`.
2. Si `weekID` courant ≠ `questWeekID` : archiver les quêtes (complétées → `completedQuestIDs`), retirer les inachevées sans message, `weeklyDraw` de la nouvelle semaine.
3. Évaluer les badges (peut découler des clôtures).
4. Mettre à jour `lastClosedDay`.

- [ ] **Step 1 : Test (échec d'abord)** — container en mémoire + `FakeStepsService` : simuler 3 jours d'écart (2 repas/jour sous l'objectif le jour 1, au-dessus le jour 2) → vérifier XP attribué jour 1 seulement, DayLogs `closed`, quêtes renouvelées si changement de semaine.
- [ ] **Step 2 : Implémenter + PASS.** Brancher dans `NivelApp` : `.onChange(of: scenePhase)`.
- [ ] **Step 3 : Commit** — `git commit -am "feat: day closer catch-up on foreground (XP, day logs, quest renewal)"`

---

## Task 19 : Célébrations + splash animé

**Files:** Create: `App/Views/Celebrations/LevelUpView.swift` (+ `ConfettiView`), `App/Views/Splash/SplashView.swift` — Modify: `RootView.swift`

- **ConfettiView** : `TimelineView` + `Canvas`, ~80 particules (rectangles/cercles aux couleurs Theme.orange/accent/green/blue), gravité + rotation, 2,5 s.
- **LevelUpView** : plein écran overlay — fond crème 95 %, Nivelito (joy) qui saute (spring répété), "NIVEAU {n} !" en gros, sous-texte message `levelUp`, confettis, haptique `.success` (`UINotificationFeedbackGenerator`). Bouton "Continuer". Présenté quand `GameService.pendingCelebration != nil` (file d'attente si plusieurs : level-up puis badges un par un).
- Variante compacte pour badge et quête complétée (bannière en haut, 3 s).
- **SplashView** (spec §5) : fond crème, Nivelito apparaît avec un rebond (scale 0.3→1.05→1 spring), logotype "Nivel" (arrondi, bold, `Theme.text`) glisse dessous, ~1,5 s puis fondu vers le contenu ; tap = skip. Affiché à chaque lancement à froid (état dans `RootView`).

- [ ] **Step 1 : Confetti + LevelUp + bannières**, déclenchement testé en simulateur (forcer un level-up en loggant des repas).
- [ ] **Step 2 : Splash** ; vérif fluide + skip.
- [ ] **Step 3 : Commit** — `git commit -am "feat: level-up celebration, confetti and animated splash"`

---

## Task 20 : Icône d'app + README + finitions

**Files:** Create: `design/appicon.svg`, `App/Assets.xcassets/AppIcon.appiconset/` , `README.md` — Modify: divers (polish)

- [ ] **Step 1 : Icône** — `design/appicon.svg` : Nivelito (tête seule, légèrement croppée en bas) centré sur fond dégradé crème→pêche (`#fdf6ec → #f9e8d8`), SANS coins arrondis (iOS les applique). Générer le PNG :

Run: `brew list librsvg >/dev/null 2>&1 || brew install librsvg` puis `rsvg-convert -w 1024 -h 1024 design/appicon.svg -o /tmp/appicon-1024.png`
Copier dans `AppIcon.appiconset` avec un `Contents.json` single-size (`"platform": "ios", "size": "1024x1024"`). Rebuild → l'icône apparaît sur le simulateur.

- [ ] **Step 2 : README.md** — en français : présentation, captures, architecture (NivelCore/App), commandes (xcodegen, swift test, xcodebuild), **procédure re-build hebdo compte gratuit** pas à pas (ouvrir Xcode → brancher l'iPhone → sélectionner l'appareil → ⌘R ; Réglages iPhone → Général → VPN et gestion de l'appareil → faire confiance au certificat la 1ʳᵉ fois ; à refaire tous les 7 jours pour chaque téléphone), et note sur le choix du profil à l'onboarding.

- [ ] **Step 3 : Passe de finition** — checklist : aucun rouge nulle part ; tous les textes en français sans faute ; `~` devant chaque kcal estimée (spec §13) ; retours haptiques sur log/validation ; VoiceOver labels sur les jauges principales ; suite complète verte (`swift test` + `xcodebuild … test`).

- [ ] **Step 4 : Commit final** — `git commit -am "feat: app icon, README with weekly rebuild guide, final polish"`

---

## Vérification de fin de plan

1. `cd NivelCore && swift test` → tout PASS (≥ 25 tests).
2. `xcodegen generate && xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test` → BUILD + TESTS OK.
3. Parcours manuel simulateur : onboarding complet → log d'un repas < 15 s → pesée → level-up visible → quêtes/badges cohérents → réglages persistés.
4. Déploiement réel : brancher l'iPhone de Michaël, signer avec le compte gratuit, vérifier HealthKit réel (pas du jour non nuls) — étape humaine, hors agent.
