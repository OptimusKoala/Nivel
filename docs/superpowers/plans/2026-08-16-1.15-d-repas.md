# Nivel 1.15 — Lot D : Repas de brasserie — Plan d'implémentation

> **Pour les agents :** SOUS-COMPÉTENCE REQUISE — utiliser `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans` pour exécuter ce plan tâche par tâche.

**But :** trente et une entrées de registre restaurant français dans le catalogue d'aliments.

**Architecture :** de la donnée, et **rien que de la donnée**. Aucune ligne de Swift n'est écrite dans ce lot : trois blocs d'entrées dans `foods.json`, et les compteurs des tests mis à jour. Contrairement aux quatorze plats de la 1.14, ces plats n'ont **pas** de composition : voir spec §6.1, la raison y est écrite et ce plan ne la rediscute pas.

**Pile :** JSON, XCTest.

**Spec :** `docs/superpowers/specs/2026-08-16-nivel-1.15-design.md` §6. **Les quatre tables (§6.2, §6.3, §6.4) sont la source : aller les lire, ne pas les recopier depuis ce plan.**

**Dépendance :** aucune sur les autres lots.

---

## ⚠️ Quatre pièges de ce dépôt, tous dans les tests des aliments

**1. Les catégories doivent rester contiguës dans le fichier.** `testLesCategoriesSontContiguesDansLeFichier` parcourt `foods.json` dans l'ordre et échoue si une catégorie **réapparaît** après avoir été quittée. Les dix-neuf plats se glissent donc **à la fin du bloc `dish`**, les sept accompagnements à la fin du bloc `side`, les cinq desserts à la fin du bloc `dessert` — jamais en vrac à la fin du fichier.

**2. Trois compteurs sont épinglés à l'unité**, pas bornés :
- `testLaTailleDuCatalogue` : 157 entrées, dont 122 non-recettes → deviennent **188** et **153** ;
- `testChaqueOngletAfficheSonNombreDItems` : cinq comptes par onglet **et** leur somme → `dish` 30 → **49**, `side` 58 → **65**, `dessert` 10 → **15** ;
- `testLesDixDessertsSontComplets` : nom qui ment déjà, à renommer en passant à quinze.

**3. Le jeu des tags est épinglé exactement.** `testTagsAlcoolEtDessertGourmandSontExactementCesItems` compare le jeu d'items tagués à une liste littérale. Quatre des cinq nouveaux desserts prennent `richDessert` : **cette liste doit être mise à jour dans la même tâche**, sinon la suite tombe sur un test dont l'intitulé ne parle pas des desserts.

**4. L'unicité des noms est par catégorie.** `testLesNomsSontUniquesParCategorie`. C'est ce qui autorise « Gratin dauphinois » à exister en plat **et** en accompagnement. Un test dédié pinne ce doublon volontaire, pour que personne ne le « corrige » plus tard.

**Commandes de test :**
```bash
cd NivelCore && swift test
cd NivelCore && swift test --filter FoodCatalogTests
```

> **Piège de `swift test --filter`** : un nom de suite mal orthographié passe au vert en n'exécutant rien. Vérifier le nombre de tests exécutés.

Aucun fichier créé dans ce lot : **pas de `xcodegen generate`**.

---

## Structure des fichiers

| Fichier | Responsabilité | Action |
|---|---|---|
| `NivelCore/Sources/NivelCore/Resources/foods.json` | Catalogue d'aliments | Modifier : +31 entrées |
| `NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift` | Garde-fous du catalogue | Modifier : compteurs, tags, nouveaux tests |

---

## Task 1 : dix-neuf plats

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/Resources/foods.json` (fin du bloc `dish`)
- Test : `NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift`

- [ ] **Étape 1 : écrire les tests qui échouent**

```swift
// MARK: Brasserie (spec 1.15 §6)

/// Les dix-neuf plats de la spec §6.2, transcrits depuis la table. Garde-fou de
/// transcription : ces kcal viennent d'un tableau, et un tableau se recopie mal.
func testLesDixNeufPlatsDeBrasserieRetombentSurLaSpec() {
    let attendu: [String: Double] = [
        "escargots": 220, "frog_legs": 155, "steak_tartare": 180, "andouillette": 290,
        "duck_confit": 250, "duck_breast": 230, "veal_blanquette": 130,
        "beef_bourguignon": 140, "cassoulet": 165, "choucroute": 130,
        "tartiflette": 180, "raclette": 260, "fondue_savoyarde": 270,
        "moules_frites": 150, "entrecote_poivre": 230, "onion_soup": 110,
        "quiche_lorraine": 280, "sole_meuniere": 165, "coq_au_vin": 150,
    ]
    for (id, kcal) in attendu {
        let item = catalog.items.first { $0.id == id }
        XCTAssertEqual(item?.kcalPer100g, kcal, "\(id)")
        XCTAssertEqual(item?.category, .dish, "\(id)")
    }
}

/// Spec §6.1 : au restaurant on ne pèse rien, et le catalogue n'a ni escargot ni
/// grenouille à ranger sous une ligne générique. Ces plats fonctionnent au forfait,
/// comme « Autre ». Ce test existe pour que le choix reste un choix : quelqu'un qui
/// leur ajouterait une composition « par cohérence » le verrait ici.
func testLesPlatsDeBrasserieNOntPasDeComposition() throws {
    let compositions = try Catalogs.compositions()
    for id in ["escargots", "frog_legs", "andouillette", "raclette", "moules_frites"] {
        XCTAssertNil(compositions[id], "\(id) ne doit pas avoir de composition")
    }
}

/// Trois plats du soir seulement. Le catalogue cantonne déjà des plats à un créneau
/// dans l'autre sens (petit-déjeuner) : même mécanique.
func testLesPlatsDuSoirSontCantonnes() throws {
    for id in ["raclette", "fondue_savoyarde", "onion_soup"] {
        let item = try XCTUnwrap(catalog.items.first { $0.id == id })
        XCTAssertEqual(item.slots, [.dinner], id)
    }
}
```

> **Adapter** : reprendre la façon dont `FoodCatalogTests` accède déjà aux compositions (voir `testToutIngredientCiteExiste`). Le corps des assertions se reprend tel quel.

- [ ] **Étape 2 : mettre à jour les deux compteurs**

Dans le même fichier, `testLaTailleDuCatalogue` et `testChaqueOngletAfficheSonNombreDItems`. **Après cette tâche seule**, les valeurs intermédiaires sont : 176 entrées, 141 non-recettes, `dish` = 49. Les autres onglets ne bougent pas encore.

> Faire cet ajustement **maintenant** et non à la fin : un lot qui laisse la suite rouge entre deux commits interdit de bissecter.

- [ ] **Étape 3 : lancer les tests pour les voir échouer**

```bash
cd NivelCore && swift test --filter FoodCatalogTests
```

Attendu : échecs sur les kcal (item nil) et sur les compteurs.

- [ ] **Étape 4 : ajouter les dix-neuf entrées**

À la **fin du bloc `dish`** de `foods.json`, juste avant la première entrée de catégorie `side`. Format d'une entrée, calqué sur `pizza` :

```json
  {
    "id": "escargots",
    "name": "Escargots de Bourgogne",
    "emoji": "🐌",
    "kcalPer100g": 220,
    "unitLabel": null,
    "unitLabelPlural": null,
    "unitGrams": null,
    "category": "dish",
    "tags": [],
    "slots": ["lunch", "dinner"],
    "defaultGrams": 90
  },
```

Les dix-huit autres sur le même modèle, **valeurs prises dans la table de la spec §6.2**. `slots` vaut `["dinner"]` pour `raclette`, `fondue_savoyarde` et `onion_soup`, `["lunch", "dinner"]` pour les seize autres.

⚠️ Les plats-recettes de la 1.14 (`isRecipe: true`) sont eux aussi de catégorie `dish` : vérifier où finit le bloc `dish` **ordinaire** et où commencent les recettes, et insérer avant. La contiguïté porte sur la catégorie, pas sur `isRecipe`, mais garder les recettes groupées reste plus lisible.

- [ ] **Étape 5 : lancer toute la suite**

```bash
cd NivelCore && swift test
```

Attendu : **verte**. `testNoBundleResourceContainsEmDash` et `testPasDeDoublonDId` doivent passer sans intervention.

- [ ] **Étape 6 : commit**

```bash
git add NivelCore/Sources/NivelCore/Resources/foods.json \
        NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift
git commit -m "feat(repas): dix-neuf plats de brasserie francaise"
```

---

## Task 2 : sept accompagnements, dont le gratin dupliqué

**Fichiers :**
- Modifier : `foods.json` (fin du bloc `side`), `FoodCatalogTests.swift`

- [ ] **Étape 1 : écrire les tests qui échouent**

```swift
func testLesSeptAccompagnementsDeBrasserieRetombentSurLaSpec() {
    let attendu: [String: Double] = [
        "green_beans": 35, "mushrooms_pan": 70, "potatoes_sauteed": 165,
        "ratatouille": 60, "spinach_cream": 90, "rice_pilaf": 145,
        "gratin_dauphinois_side": 149,
    ]
    for (id, kcal) in attendu {
        let item = catalog.items.first { $0.id == id }
        XCTAssertEqual(item?.kcalPer100g, kcal, "\(id)")
        XCTAssertEqual(item?.category, .side, "\(id)")
    }
}

/// Doublon VOULU (spec §6.3) : le même gratin existe en plat et en accompagnement,
/// pour qu'il puisse accompagner une viande. Les deux kcal sont épinglées ENSEMBLE :
/// deux valeurs différentes pour le même gratin seraient un défaut, pas une nuance.
/// `testLesNomsSontUniquesParCategorie` autorise ce doublon, l'unicité étant par
/// catégorie ; ce test-ci empêche qu'on le « corrige » un jour par mégarde.
func testLeGratinDauphinoisExisteDansLesDeuxCategories() throws {
    let plat = try XCTUnwrap(catalog.items.first { $0.id == "gratin_dauphinois" })
    let accompagnement = try XCTUnwrap(catalog.items.first { $0.id == "gratin_dauphinois_side" })
    XCTAssertEqual(plat.category, .dish)
    XCTAssertEqual(accompagnement.category, .side)
    XCTAssertEqual(plat.name, accompagnement.name)
    XCTAssertEqual(plat.kcalPer100g, accompagnement.kcalPer100g)
}
```

- [ ] **Étape 2 : mettre à jour les compteurs** — 183 entrées, 148 non-recettes, `side` = 65.

- [ ] **Étape 3 : lancer, voir échouer** — `cd NivelCore && swift test --filter FoodCatalogTests`

- [ ] **Étape 4 : ajouter les sept entrées** à la fin du bloc `side`, valeurs de la spec §6.3, `slots: []` et `tags: []` pour toutes (les accompagnements n'ont pas de créneau, cf. `fries` et `green_veg`).

- [ ] **Étape 5 : lancer toute la suite** — `cd NivelCore && swift test`, verte.

- [ ] **Étape 6 : commit**

```bash
git add NivelCore/Sources/NivelCore/Resources/foods.json \
        NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift
git commit -m "feat(repas): sept accompagnements, gratin dauphinois compris"
```

---

## Task 3 : cinq desserts

**Fichiers :**
- Modifier : `foods.json` (fin du bloc `dessert`), `FoodCatalogTests.swift`

- [ ] **Étape 1 : écrire les tests qui échouent**

```swift
func testLesCinqDessertsDeBrasserieRetombentSurLaSpec() throws {
    let attendu: [String: (kcal: Double, unite: String, poids: Int)] = [
        "creme_brulee":     (250, "ramequin", 120),
        "tarte_tatin":      (250, "part", 120),
        "profiteroles":     (300, "part", 130),
        "ile_flottante":    (130, "part", 130),
        "fondant_chocolat": (400, "part", 100),
    ]
    for (id, valeurs) in attendu {
        let item = try XCTUnwrap(catalog.items.first { $0.id == id }, id)
        XCTAssertEqual(item.kcalPer100g, valeurs.kcal, id)
        XCTAssertEqual(item.unitLabel, valeurs.unite, id)
        XCTAssertEqual(item.unitGrams, valeurs.poids, id)
        XCTAssertEqual(item.category, .dessert, id)
    }
}

/// Quatre sur cinq seulement (spec §6.4) : à 130 kcal aux 100 g, l'île flottante est
/// plus légère que la compote, qui n'est pas taguée non plus.
func testLIleFlottanteNEstPasUnDessertGourmand() throws {
    let ile = try XCTUnwrap(catalog.items.first { $0.id == "ile_flottante" })
    XCTAssertFalse(ile.tags.contains("richDessert"))
}
```

- [ ] **Étape 2 : mettre à jour les compteurs ET le jeu de tags**

- `testLaTailleDuCatalogue` : **188** entrées, **153** non-recettes ;
- `testChaqueOngletAfficheSonNombreDItems` : `dessert` = **15**, et la somme finale `15 + 9 + 49 + 65 + 15` ;
- `testLesDixDessertsSontComplets` : renommer en `testLesQuinzeDessertsSontComplets` et compléter la liste ;
- `testTagsAlcoolEtDessertGourmandSontExactementCesItems` : ajouter `creme_brulee`, `tarte_tatin`, `profiteroles`, `fondant_chocolat` au jeu attendu — **pas** `ile_flottante`.

- [ ] **Étape 3 : lancer, voir échouer** — `cd NivelCore && swift test --filter FoodCatalogTests`

- [ ] **Étape 4 : ajouter les cinq entrées** à la fin du bloc `dessert`, valeurs de la spec §6.4, sur le modèle de `ice_cream` (qui porte une unité, un poids d'unité et le tag).

- [ ] **Étape 5 : lancer toute la suite**

```bash
cd NivelCore && swift test
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

Attendu : les deux vertes. La cible app est lancée ici parce que `RecipeStripTests` et `MealLoggingTests` traversent le catalogue.

- [ ] **Étape 6 : commit**

```bash
git add NivelCore/Sources/NivelCore/Resources/foods.json \
        NivelCore/Tests/NivelCoreTests/FoodCatalogTests.swift
git commit -m "feat(repas): cinq desserts de brasserie"
```

---

## Vérification finale du lot

- [ ] `cd NivelCore && swift test` : verte, 188 entrées, 153 aliments ordinaires.
- [ ] `xcodebuild ... test` : cible entière verte.
- [ ] Dans le simulateur, feuille de saisie d'un repas : l'onglet **Plats** montre les dix-neuf nouveaux, l'onglet **Ingrédients** les sept, l'onglet **Desserts** les cinq. Les emoji s'affichent tous (aucun carré vide).
- [ ] Chercher « gratin » dans la recherche d'aliments : **deux** résultats, un par catégorie. C'est le comportement voulu.
- [ ] Ouvrir la raclette : elle n'est proposée qu'au dîner.
