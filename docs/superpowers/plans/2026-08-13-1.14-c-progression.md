# Nivel 1.14 — Lot C : Progression et dépense — Plan d'implémentation

> **Pour les agents :** SOUS-COMPÉTENCE REQUISE — utiliser `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans` pour exécuter ce plan tâche par tâche.

**But :** durcir la courbe de niveaux sans faire descendre personne, montrer un objectif de dépense calorique dans un deuxième anneau, et porter les badges de 24 à 38.

**Architecture :** la courbe est une formule pure dans `LevelSystem` ; l'ancienne survit sous le nom `legacyXPRequired` pour le seul usage de la migration, qui recharge l'XP une fois et se marque faite par un `levelCurveVersion` sur `GamificationState`. La dépense est un calcul pur (`BurnCalculator`) qui somme les kcal des pas et celles des activités **non** marchées, en redécomposant les séances mixtes par leurs étapes de catalogue.

**Pile :** Swift 6, SwiftUI, SwiftData (migration légère), XCTest.

**Spec :** `docs/superpowers/specs/2026-08-13-nivel-1.14-design.md` §5.

**Dépendance dure : le lot B EN ENTIER.** Ce plan a besoin de `Activity.stepsBased` (B/Task 1), de l'activité `pushups` (B/Task 2), de `MuscuCatalog` (B/Task 4), de `XPAction.muscuSessionDone` et `ActivityKind.muscu` (B/Task 5), et de `MuscuPlanSettings` (B/Task 6). Aucun de ces symboles n'existe aujourd'hui. **Ne pas démarrer ce plan avant que le lot B soit vert** (les illustrations de B/Task 7 sont la seule exception : elles ne bloquent que `SportAssetsTests`).

**Le projet Xcode est généré.** `Nivel.xcodeproj` n'a aucun groupe synchronisé : tout fichier créé exige **`xcodegen generate`** avant le moindre `xcodebuild`, sinon un test neuf ne s'exécute pas — et « suite introuvable » ressemble à s'y tromper au rouge attendu du TDD, puis à un faux vert.

⚠️ **La Task 2 n'a pas de seconde chance.** Un bug de migration fait perdre un niveau sans moyen de le retrouver. Elle s'écrit en test d'abord, sans exception.

> **Noms de test en français.** Les extraits de test de ce plan portent des noms anglais (`testDessertCategoryExists`) : c'est une facilité de rédaction, pas une consigne. Les suites du dépôt sont en français (`testPasDeDoublonDId`, `testUniteEtPoidsVontEnsemble`) — **traduire chaque nom de test au moment de l'écrire**. Le corps et les assertions, eux, se reprennent tels quels.

**Commandes de test :**
```bash
cd NivelCore && swift test
cd NivelCore && swift test --filter LevelSystemTests
cd NivelCore && swift test --filter BurnCalculatorTests
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

---

## Structure des fichiers

| Fichier | Responsabilité | Action |
|---|---|---|
| `NivelCore/Sources/NivelCore/LevelSystem.swift` | Seuils d'XP par niveau | Modifier : nouvelle formule + `legacyXPRequired` |
| `NivelCore/Sources/NivelCore/BurnCalculator.swift` | Dépense du jour | **Créer** |
| `NivelCore/Sources/NivelCore/CalorieCalculator.swift` | Objectifs caloriques | Modifier : `dailyBurnTarget` |
| `NivelCore/Sources/NivelCore/BadgeEngine.swift` | Métriques de badges | Modifier : trois métriques |
| `NivelCore/Sources/NivelCore/Resources/badges.json` | 24 → 38 badges | Modifier |
| `NivelCore/Sources/NivelCore/XPEngine.swift` | Barème d'XP | Modifier : `muscuSessionDone` |
| `NivelCore/Sources/NivelCore/QuestEngine.swift` | Tirage hebdo | Modifier : `muscuAvailable` |
| `NivelCore/Sources/NivelCore/Resources/quests.json` | 21 → 25 quêtes | Modifier |
| `App/Models/PersistentModels.swift` | Modèles SwiftData | Modifier : 3 champs |
| `App/Services/GameService.swift` | Migration, stats de badges | Modifier |
| `App/Services/DayCloser.swift` | Clôture du jour | Modifier : dépense du jour |
| `App/Views/Home/CalorieRing.swift` | L'anneau | Modifier : deuxième anneau |
| `App/Views/Home/HomeView.swift` | Accueil | Modifier : passe la dépense |
| `App/Views/Settings/SettingsView.swift` | Réglages | Modifier : objectif de dépense |

---

## Task 1 : la nouvelle courbe

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/LevelSystem.swift`
- Test : `NivelCore/Tests/NivelCoreTests/LevelSystemTests.swift`

- [ ] **Étape 1 : réécrire les tests existants pour la nouvelle courbe**

Les valeurs actuelles de `testThresholds` et `testLevelForXP` sont celles de l'ancienne formule : elles doivent changer, et **c'est la seule fois du lot où l'on modifie un test au lieu du code**.

```swift
func testThresholds() {
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 1), 0)
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 2), 70)      // 70×1^2,2
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 3), 322)     // 70×2^2,2 = 321,6
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 4), 785)
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 5), 1478)
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 10), 8799)
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 20), 45536)
    XCTAssertEqual(LevelSystem.xpRequired(forLevel: 30), 115445)
}

func testLevelForXP() {
    XCTAssertEqual(LevelSystem.level(forXP: 0), 1)
    XCTAssertEqual(LevelSystem.level(forXP: 69), 1)
    XCTAssertEqual(LevelSystem.level(forXP: 70), 2)
    XCTAssertEqual(LevelSystem.level(forXP: 321), 2)
    XCTAssertEqual(LevelSystem.level(forXP: 322), 3)
}

/// Les deux premiers niveaux arrivent PLUS vite qu'avant ; le croisement se fait
/// au niveau 4. C'est voulu : le premier jour récompense davantage, et le
/// freinage commence là où la gêne commençait.
func testEarlyLevelsAreCheaperThanBefore() {
    XCTAssertLessThan(LevelSystem.xpRequired(forLevel: 2), LevelSystem.legacyXPRequired(forLevel: 2))
    XCTAssertLessThan(LevelSystem.xpRequired(forLevel: 3), LevelSystem.legacyXPRequired(forLevel: 3))
    XCTAssertGreaterThan(LevelSystem.xpRequired(forLevel: 4), LevelSystem.legacyXPRequired(forLevel: 4))
}

/// L'ancienne formule doit rester EXACTE : la migration s'en sert pour lire le
/// niveau déjà atteint. La modifier ferait perdre un niveau à quelqu'un.
func testLegacyFormulaIsPreserved() {
    XCTAssertEqual(LevelSystem.legacyXPRequired(forLevel: 2), 246)
    XCTAssertEqual(LevelSystem.legacyXPRequired(forLevel: 10), 1995)
    XCTAssertEqual(LevelSystem.legacyXPRequired(forLevel: 20), 4913)
}

/// Invariant de forme, indépendant des valeurs : un niveau coûte toujours plus
/// que le précédent, et l'écart ne se resserre jamais.
func testCurveIsStrictlyIncreasingAndAccelerating() {
    for level in 2..<60 {
        let previous = LevelSystem.xpRequired(forLevel: level - 1)
        let current = LevelSystem.xpRequired(forLevel: level)
        let next = LevelSystem.xpRequired(forLevel: level + 1)
        XCTAssertGreaterThan(current, previous, "niveau \(level)")
        XCTAssertGreaterThanOrEqual(next - current, current - previous, "niveau \(level)")
    }
}

func testProgressWithinLevel() {
    // à 322 XP : début du niveau 3 → 0 gagné, il en faut 785 − 322 pour le 4.
    let p = LevelSystem.progress(forXP: 322)
    XCTAssertEqual(p.current, 0)
    XCTAssertEqual(p.needed, 785 - 322)
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter LevelSystemTests`
Attendu : ÉCHEC — seuil 246 au lieu de 70, et `legacyXPRequired` inconnu.

- [ ] **Étape 3 : implémenter**

```swift
public enum LevelSystem {
    /// XP total requis pour ATTEINDRE le niveau n (niveau 1 = 0 XP).
    /// Seuil = 70 × (n−1)^2,2 (spec v1.14 §5.1).
    ///
    /// L'exposant a été porté de 1,3 à 2,2 et la base décalée d'un niveau : sous
    /// l'ancienne courbe le niveau 30 tombait en un mois d'usage régulier, donc un
    /// niveau ne voulait plus rien dire. Le décalage rend les niveaux 2 et 3 PLUS
    /// rapides qu'avant (70 et 322 au lieu de 246 et 417) : le premier jour
    /// récompense davantage, et le freinage ne commence qu'au niveau 4.
    public static func xpRequired(forLevel level: Int) -> Int {
        guard level > 1 else { return 0 }
        return Int((70 * pow(Double(level - 1), 2.2)).rounded())
    }

    /// L'ancienne courbe (100 × n^1,3), conservée pour UN seul appelant légitime :
    /// la migration de `GameService`, qui doit lire le niveau atteint sous
    /// l'ancien barème pour ne faire descendre personne (spec §5.2). Elle ne peut
    /// donc pas être supprimée, et la modifier ferait perdre un niveau à
    /// quelqu'un : ne pas y toucher.
    public static func legacyXPRequired(forLevel level: Int) -> Int {
        guard level > 1 else { return 0 }
        return Int((100 * pow(Double(level), 1.3)).rounded())
    }

    /// Niveau atteint sous l'ancienne courbe — même usage unique que ci-dessus.
    public static func legacyLevel(forXP xp: Int) -> Int {
        var level = 1
        while legacyXPRequired(forLevel: level + 1) <= xp { level += 1 }
        return level
    }

    // level(forXP:) et progress(forXP:) inchangés : ils ne lisent que xpRequired.
}
```

- [ ] **Étape 4 : lancer toute la suite**

Run : `cd NivelCore && swift test`
Attendu : SUCCÈS. Si `XPEngineTests` ou `QuestEngineTests` calculaient un niveau attendu, corriger **leurs valeurs**, jamais la formule.

- [ ] **Étape 5 : commit**

```bash
git add NivelCore/Sources/NivelCore/LevelSystem.swift \
        NivelCore/Tests/NivelCoreTests/LevelSystemTests.swift
git commit -m "feat(core): la courbe de niveaux freine enfin"
```

---

## Task 2 : la migration, sans descente

**Fichiers :**
- Modifier : `App/Models/PersistentModels.swift:171-203`
- Modifier : `App/Services/GameService.swift`
- Test : `NivelTests/LevelMigrationTests.swift` (**créer**)

- [ ] **Étape 1 : écrire les tests qui échouent**

```swift
// NivelTests/LevelMigrationTests.swift
// Recharge d'XP au premier lancement de la 1.14 (spec §5.2). Un bug ici fait
// perdre un niveau sans moyen de le retrouver : cette suite passe AVANT le code.
import XCTest
import SwiftData
import NivelCore
@testable import Nivel

@MainActor
final class LevelMigrationTests: XCTestCase {
    private var container: ModelContainer!

    override func setUpWithError() throws {
        let schema = Schema([UserProfile.self, MealEntry.self, WeightEntry.self,
                             DayLog.self, GamificationState.self, ActivityEntry.self])
        container = try ModelContainer(
            for: schema,
            configurations: [ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)]
        )
    }

    override func tearDown() { container = nil }

    private func makeService(totalXP: Int, curveVersion: Int) -> (GameService, GamificationState) {
        let context = container.mainContext
        let state = GamificationState(totalXP: totalXP)
        state.levelCurveVersion = curveVersion
        context.insert(state)
        try? context.save()
        let service = GameService(modelContext: context, stepsService: FakeStepsService(),
                                  widgetDefaults: nil)
        return (service, state)
    }

    /// Le cas de Michaël : niveau 13 sous l'ancienne courbe (3 000 XP).
    /// Sans recharge il retomberait au niveau 6.
    func testExistingPlayerKeepsTheirLevel() {
        let (service, state) = makeService(totalXP: 3000, curveVersion: 1)
        XCTAssertEqual(LevelSystem.legacyLevel(forXP: 3000), 13)
        service.migrateLevelCurveIfNeeded()
        XCTAssertEqual(LevelSystem.level(forXP: state.totalXP), 13)
        XCTAssertEqual(state.totalXP, LevelSystem.xpRequired(forLevel: 13))  // 16 569
        XCTAssertEqual(state.levelCurveVersion, 2)
    }

    /// Une deuxième exécution ne doit RIEN recharger : sinon chaque lancement
    /// offrirait un niveau.
    func testMigrationRunsOnlyOnce() {
        let (service, state) = makeService(totalXP: 3000, curveVersion: 1)
        service.migrateLevelCurveIfNeeded()
        let afterFirst = state.totalXP
        service.migrateLevelCurveIfNeeded()
        XCTAssertEqual(state.totalXP, afterFirst)
    }

    /// Un profil neuf n'est pas touché : 0 XP reste 0 XP, pas de cadeau.
    func testFreshProfileIsUntouched() {
        let (service, state) = makeService(totalXP: 0, curveVersion: 1)
        service.migrateLevelCurveIfNeeded()
        XCTAssertEqual(state.totalXP, 0)
        XCTAssertEqual(LevelSystem.level(forXP: state.totalXP), 1)
    }

    /// LE test qui ferme le trou le plus vicieux du lot. `GameService` se construit
    /// au lancement, AVANT l'onboarding : l'état de gamification n'existe pas
    /// encore. S'il naissait ensuite en version 1, le lancement suivant
    /// « rechargerait » une XP déjà gagnée sous la NOUVELLE courbe — 3 000 XP
    /// deviendraient 16 569, sept niveaux offerts sans rien faire.
    func testANewStateIsBornOnTheNewCurve() {
        let state = GamificationState()
        XCTAssertEqual(state.levelCurveVersion, 2,
                       "un état créé par le code de la 1.14 n'est jamais candidat à la recharge")
    }

    /// Le scénario complet d'une installation neuve : état créé par l'onboarding,
    /// XP gagnée normalement, relance. Rien ne bouge.
    func testFreshInstallEarningXPIsNeverMigrated() {
        let context = container.mainContext
        let state = GamificationState()          // comme l'onboarding le crée
        context.insert(state)
        state.totalXP = 3000                     // trois semaines de jeu, nouvelle courbe
        try? context.save()
        let service = GameService(modelContext: context, stepsService: FakeStepsService(),
                                  widgetDefaults: nil)
        service.migrateLevelCurveIfNeeded()
        XCTAssertEqual(state.totalXP, 3000, "aucun niveau offert")
        XCTAssertEqual(LevelSystem.level(forXP: state.totalXP), 6)
    }

    /// La recharge ne RETIRE jamais d'XP, même quand la nouvelle courbe est plus
    /// généreuse (niveaux 2 et 3) : `max` et non affectation sèche.
    func testMigrationNeverRemovesXP() {
        let (service, state) = makeService(totalXP: 300, curveVersion: 1)  // ancien L2
        service.migrateLevelCurveIfNeeded()
        XCTAssertEqual(state.totalXP, 300)
        XCTAssertGreaterThanOrEqual(LevelSystem.level(forXP: state.totalXP), 2)
    }

    /// Un joueur déjà migré (version 2) n'est jamais retouché, quel que soit son XP.
    func testAlreadyMigratedIsSkipped() {
        let (service, state) = makeService(totalXP: 500, curveVersion: 2)
        service.migrateLevelCurveIfNeeded()
        XCTAssertEqual(state.totalXP, 500)
    }
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

```bash
xcodegen generate      # le fichier de test est NEUF : sans ça il n'est pas compilé
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -only-testing:NivelTests/LevelMigrationTests test
```
Attendu : ÉCHEC de compilation — ni `levelCurveVersion` ni `migrateLevelCurveIfNeeded`.

⚠️ Sans `xcodegen generate`, le rouge observé serait « suite de tests introuvable » — indiscernable du rouge attendu, puis **faux vert** ensuite, la suite ne tournant jamais.

- [ ] **Étape 3 : implémenter**

Dans `PersistentModels.swift`, sur `GamificationState` :

```swift
// Défaut au niveau de la DÉCLARATION : requis pour la migration légère SwiftData
// des stores existants (le défaut de l'init ne suffit pas — leçon de
// completedThisWeekQuestIDs en v1, reconfirmée par reminderTimes en v1.9).
// 1 = courbe d'avant la 1.14, 2 = courbe durcie avec recharge appliquée.
var levelCurveVersion: Int = 1
```

**Et le défaut de l'`init` vaut 2**, ce qui n'est pas une redondance mais la moitié utile du dispositif :

```swift
init(
    totalXP: Int = 0,
    // ...
    // 2 et NON 1 : le défaut de déclaration ci-dessus sert aux stores d'avant la
    // 1.14, qui n'ont pas la clé. Un état créé PAR le code de la 1.14 (onboarding)
    // naît, lui, sur la nouvelle courbe. Sans cette asymétrie, une installation
    // neuve se ferait « recharger » au lancement suivant une XP déjà gagnée sous la
    // nouvelle courbe : sept niveaux offerts à 3 000 XP.
    levelCurveVersion: Int = 2,
    lastClosedDay: Date? = nil
)
```

Dans `GameService` :

```swift
/// Recharge d'XP unique au passage à la courbe durcie (spec §5.2).
///
/// La nouvelle courbe exige ~4 fois plus d'XP au niveau 10 : appliquée brute, elle
/// ferait DESCENDRE tous les joueurs existants — la seule régression que l'app se
/// serait jamais autorisée, contre sa règle « zéro pression, jamais de score
/// négatif ». On porte donc l'XP au seuil que le niveau déjà atteint exige sous la
/// nouvelle courbe : personne ne descend, et le niveau suivant se mérite au
/// nouveau rythme. Le total affiché fait un bond visible ; c'est le prix, et il
/// vaut mieux qu'un plateau de plusieurs semaines.
///
/// `max` et non affectation sèche : aux niveaux 2 et 3 la nouvelle courbe est plus
/// GÉNÉREUSE, et une affectation retirerait de l'XP.
func migrateLevelCurveIfNeeded() {
    guard let state = fetchState(), state.levelCurveVersion < 2 else { return }
    let reached = LevelSystem.legacyLevel(forXP: state.totalXP)
    state.totalXP = max(state.totalXP, LevelSystem.xpRequired(forLevel: reached))
    state.levelCurveVersion = 2
    try? modelContext.save()
}
```

**Utiliser `fetchState()`, jamais `fetchOrCreateState()`.** `GameService` expose les deux ; le second **insère** un `GamificationState` s'il n'en trouve pas, et l'appeler depuis l'init en créerait un à chaque construction du service — previews, `ScreenshotMode`, plusieurs suites de tests — en violation de l'invariant écrit sur `fetchState` (« le snapshot widget ne doit RIEN insérer »). Une installation neuve n'a pas d'état : la migration doit simplement ne rien faire, ce que le `guard let` assure.

Appeler `migrateLevelCurveIfNeeded()` **dans l'init de `GameService`, après le chargement de l'état** — avant tout affichage, donc avant que quiconque puisse voir un niveau faux.

- [ ] **Étape 4 : lancer les tests**

Run : `xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : SUCCÈS, les six cas de migration compris.

- [ ] **Étape 5 : commit**

```bash
git add App/Models/PersistentModels.swift App/Services/GameService.swift \
        NivelTests/LevelMigrationTests.swift Nivel.xcodeproj
git commit -m "feat(app): la courbe durcie ne fait descendre personne"
```

---

## Task 3 : l'objectif de dépense

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/CalorieCalculator.swift`
- Modifier : `App/Models/PersistentModels.swift` (`UserProfile`)
- Modifier : `App/Views/Settings/SettingsView.swift`
- Test : `NivelCore/Tests/NivelCoreTests/CalorieCalculatorTests.swift`

- [ ] **Étape 1 : écrire le test qui échoue**

```swift
func testDailyBurnTarget() {
    XCTAssertEqual(CalorieCalculator.dailyBurnTarget(weightKg: 70), 300)
    XCTAssertEqual(CalorieCalculator.dailyBurnTarget(weightKg: 90), 350)
    XCTAssertEqual(CalorieCalculator.dailyBurnTarget(weightKg: 110), 450)
    // Bornes : ni décourageant, ni irréaliste.
    XCTAssertEqual(CalorieCalculator.dailyBurnTarget(weightKg: 40), 200)
    XCTAssertEqual(CalorieCalculator.dailyBurnTarget(weightKg: 200), 600)
}

/// Multiple de 50 quel que soit le poids : un objectif affiché « 347 kcal »
/// prétendrait à une précision que ce calcul n'a pas.
func testBurnTargetIsAlwaysRoundedToFifty() {
    for weight in stride(from: 40.0, through: 200.0, by: 0.5) {
        XCTAssertEqual(CalorieCalculator.dailyBurnTarget(weightKg: weight) % 50, 0, "\(weight)")
    }
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter CalorieCalculatorTests`
Attendu : ÉCHEC — `dailyBurnTarget` inconnu.

- [ ] **Étape 3 : implémenter**

```swift
/// Objectif de dépense quotidienne (spec v1.14 §5.3) : poids × 4, arrondi à la
/// cinquantaine, borné [200, 600].
///
/// Proportionnel au poids, parce qu'un même mouvement ne coûte pas la même chose à
/// 60 et à 110 kg. Bornes assumées : sous 200 l'objectif ne veut rien dire, au-delà
/// de 600 il demande une heure de sport par jour et devient un reproche quotidien.
public static func dailyBurnTarget(weightKg: Double) -> Int {
    let raw = weightKg * 4
    let rounded = Int((raw / 50).rounded()) * 50
    return min(600, max(200, rounded))
}
```

Sur `UserProfile` :

```swift
// Défaut sur la DÉCLARATION (migration légère SwiftData). 0 = jamais réglé, donc
// la valeur calculée depuis le poids fait foi (spec §5.3).
var dailyBurnTarget: Int = 0
```

Dans `SettingsView`, un pas-à-pas ou un champ numérique à côté de l'objectif calorique et de l'objectif de pas, **même présentation que les deux existants**. L'onboarding ne pose aucune question de plus.

- [ ] **Étape 4 : lancer les tests**

Run : `cd NivelCore && swift test && xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : SUCCÈS.

- [ ] **Étape 5 : commit**

```bash
git add NivelCore/Sources/NivelCore/CalorieCalculator.swift \
        NivelCore/Tests/NivelCoreTests/CalorieCalculatorTests.swift \
        App/Models/PersistentModels.swift App/Views/Settings/SettingsView.swift
git commit -m "feat(core): un objectif de dépense, proportionnel au poids"
```

---

## Task 4 : `BurnCalculator`

**Fichiers :**
- Créer : `NivelCore/Sources/NivelCore/BurnCalculator.swift`
- Test : `NivelCore/Tests/NivelCoreTests/BurnCalculatorTests.swift` (**créer**)

C'est la tâche la plus subtile du lot : **un anneau faux ne lève aucune erreur**, seuls ces tests le voient.

- [ ] **Étape 1 : écrire les tests qui échouent**

```swift
// NivelCore/Tests/NivelCoreTests/BurnCalculatorTests.swift
// Dépense du jour (spec §5.4) : pas + activités NON marchées. Le double comptage
// est silencieux — c'est cette suite, et elle seule, qui l'empêche.
import XCTest
@testable import NivelCore

final class BurnCalculatorTests: XCTestCase {
    private let weight = 90.0

    private func activities() throws -> [String: Activity] {
        Dictionary(try Catalogs.activities().map { ($0.id, $0) }, uniquingKeysWith: { a, _ in a })
    }
    private func sessions() throws -> [String: ActivitySession] {
        Dictionary(try Catalogs.sessions().map { ($0.id, $0) }, uniquingKeysWith: { a, _ in a })
    }

    func testStepsAlone() throws {
        let kcal = BurnCalculator.kcal(steps: 8000, weightKg: weight, entries: [],
                                      activitiesByID: try activities(),
                                      sessionsByID: try sessions())
        XCTAssertEqual(kcal, 411)   // 8000 × 0,04 × 90/70
    }

    func testHomeActivityAddsUp() throws {
        let entry = BurnEntry(kind: .activity, refID: "pushups", estimatedKcal: 35)
        let kcal = BurnCalculator.kcal(steps: 8000, weightKg: weight, entries: [entry],
                                      activitiesByID: try activities(),
                                      sessionsByID: try sessions())
        XCTAssertEqual(kcal, 411 + 35)
    }

    /// LE cas qui motive tout : une marche validée est déjà dans les pas.
    func testWalkIsNotCountedTwice() throws {
        let entry = BurnEntry(kind: .activity, refID: "walk", estimatedKcal: 160)
        let kcal = BurnCalculator.kcal(steps: 8000, weightKg: weight, entries: [entry],
                                      activitiesByID: try activities(),
                                      sessionsByID: try sessions())
        XCTAssertEqual(kcal, 411, "la marche entre par les pas, pas deux fois")
    }

    /// `fresh_air` est une marche de 15 min : la séance ne doit RIEN ajouter.
    func testFullyWalkingSessionAddsNothing() throws {
        let entry = BurnEntry(kind: .dailySession, refID: "fresh_air", estimatedKcal: 60)
        let kcal = BurnCalculator.kcal(steps: 8000, weightKg: weight, entries: [entry],
                                      activitiesByID: try activities(),
                                      sessionsByID: try sessions())
        XCTAssertEqual(kcal, 411)
    }

    /// `home_cardio` mélange escaliers (marchés) et squats + étirements (non).
    /// 6×8 = 48 marchés, exclus ; 3×5,5 + 4×2,5 = 26,5 gardés.
    func testMixedSessionIsDecomposed() throws {
        let entry = BurnEntry(kind: .dailySession, refID: "home_cardio", estimatedKcal: 74)
        let kcal = BurnCalculator.kcal(steps: 8000, weightKg: weight, entries: [entry],
                                      activitiesByID: try activities(),
                                      sessionsByID: try sessions())
        XCTAssertEqual(kcal, 411 + 27, "26,5 arrondi une seule fois, à la fin")
    }

    /// Séance retirée du catalogue depuis : repli sur le total stocké plutôt que
    /// de faire disparaître une dépense réelle.
    func testUnknownSessionFallsBackToStoredTotal() throws {
        let entry = BurnEntry(kind: .muscu, refID: "disparue", estimatedKcal: 120)
        let kcal = BurnCalculator.kcal(steps: 8000, weightKg: weight, entries: [entry],
                                      activitiesByID: try activities(),
                                      sessionsByID: try sessions())
        XCTAssertEqual(kcal, 411 + 120)
    }

    /// HealthKit refusé : plus de doublon possible, donc TOUT compte — une
    /// dépense amputée serait un mensonge par omission.
    func testWithoutStepsEverythingCounts() throws {
        let entries = [BurnEntry(kind: .activity, refID: "walk", estimatedKcal: 160),
                       BurnEntry(kind: .dailySession, refID: "fresh_air", estimatedKcal: 60)]
        let kcal = BurnCalculator.kcal(steps: nil, weightKg: weight, entries: entries,
                                      activitiesByID: try activities(),
                                      sessionsByID: try sessions())
        XCTAssertEqual(kcal, 220)
    }

    func testNothingAtAll() throws {
        XCTAssertEqual(BurnCalculator.kcal(steps: nil, weightKg: weight, entries: [],
                                           activitiesByID: try activities(),
                                           sessionsByID: try sessions()), 0)
    }
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter BurnCalculatorTests`
Attendu : ÉCHEC — `BurnCalculator` n'existe pas.

- [ ] **Étape 3 : implémenter**

```swift
// NivelCore/Sources/NivelCore/BurnCalculator.swift
// Dépense calorique du jour (spec v1.14 §5.4) : pas + activités NON marchées.
// Pur, le barème est injecté — testable sans HealthKit ni SwiftData.
//
// Ces kcal restent INDICATIVES et ne sont jamais créditées au budget alimentaire
// (règle v1 §2) : une heure de marche n'autorise pas une pizza. L'estimation est
// fausse de 30 %, et la créditer en ferait un permis de manger.

import Foundation

/// Ce que `BurnCalculator` a besoin de savoir d'une validation sport. Volontairement
/// pas `ActivityEntry` : le paquet ne connaît pas SwiftData.
public struct BurnEntry: Sendable, Equatable {
    public let kind: ActivityKind
    public let refID: String
    public let estimatedKcal: Int
    public init(kind: ActivityKind, refID: String, estimatedKcal: Int) {
        self.kind = kind
        self.refID = refID
        self.estimatedKcal = estimatedKcal
    }
}

public enum BurnCalculator {
    /// `steps` à nil = pas indisponibles (HealthKit refusé, ou lecture pas encore
    /// revenue). Aucun doublon n'est alors possible, donc tout compte.
    public static func kcal(
        steps: Int?,
        weightKg: Double,
        entries: [BurnEntry],
        activitiesByID: [String: Activity],
        sessionsByID: [String: ActivitySession]
    ) -> Int {
        let stepsKcal = steps.map { StepsEstimator.kcal(steps: $0, weightKg: weightKg) } ?? 0
        let excludesWalking = steps != nil

        // Un seul arrondi, à la fin (même règle que MealEstimator) : arrondir
        // séance par séance ferait dériver une journée chargée de plusieurs kcal.
        let fromEntries = entries.reduce(0.0) { total, entry in
            total + contribution(of: entry, excludesWalking: excludesWalking,
                                 activitiesByID: activitiesByID, sessionsByID: sessionsByID)
        }
        return stepsKcal + Int(fromEntries.rounded())
    }

    private static func contribution(
        of entry: BurnEntry,
        excludesWalking: Bool,
        activitiesByID: [String: Activity],
        sessionsByID: [String: ActivitySession]
    ) -> Double {
        guard excludesWalking else { return Double(entry.estimatedKcal) }

        if entry.kind == .activity {
            // Activité inconnue du catalogue : on garde son total, comme pour une
            // séance disparue — mieux vaut un chiffre daté qu'une dépense effacée.
            let isWalking = activitiesByID[entry.refID]?.stepsBased ?? false
            return isWalking ? 0 : Double(entry.estimatedKcal)
        }

        // Séance : une entrée ne stocke qu'un TOTAL, or `fresh_air` est une marche
        // et `home_cardio` contient des escaliers. L'inclure ou l'exclure en bloc
        // serait faux dans les deux sens : on la redécompose par ses étapes.
        guard let session = sessionsByID[entry.refID] else {
            return Double(entry.estimatedKcal)
        }
        return session.steps.reduce(0.0) { total, step in
            guard let activity = activitiesByID[step.activityID], !activity.stepsBased else {
                return total
            }
            return total + activity.kcalPerMin * Double(step.minutes)
        }
    }
}
```

- [ ] **Étape 4 : lancer les tests**

Run : `cd NivelCore && swift test`
Attendu : SUCCÈS, les huit cas compris.

- [ ] **Étape 5 : commit**

```bash
git add NivelCore/Sources/NivelCore/BurnCalculator.swift \
        NivelCore/Tests/NivelCoreTests/BurnCalculatorTests.swift
git commit -m "feat(core): la dépense du jour, sans compter la marche deux fois"
```

---

## Task 5 : la dépense dans le jour clôturé

**Fichiers :**
- Modifier : `App/Models/PersistentModels.swift` (`DayLog`)
- Modifier : `App/Services/DayCloser.swift`
- Modifier : `App/Services/GameService.swift` (assemblage des `BurnEntry`)
- Test : `NivelTests/DayCloserTests.swift`

- [ ] **Étape 1 : écrire le test qui échoue**

Dans `DayCloserTests`, **en réutilisant les fixtures de la suite** (`today`, `day1…day3`, profil à 90 kg, container en mémoire, `closeOpenDays(today:)`) — les nommer précisément en les lisant, plutôt que d'inventer un décor :

```swift
/// 8 000 pas à 90 kg = 411 kcal, au-dessus de l'objectif de 350 : la dépense est
/// enregistrée ET l'objectif marqué atteint.
func testClosingADayRecordsItsBurn() async throws {
    // arrange : un jour passé avec ses pas, via le FakeStepsService de la suite
    // (voir comment les autres cas de DayCloserTests l'alimentent).
    await closer.closeOpenDays(today: today)
    let log = try fetchDayLog(day1)
    XCTAssertEqual(log.kcalBurned, 411)
    XCTAssertTrue(log.burnTargetReached)
}

/// 2 000 pas = 103 kcal, sous l'objectif : rien n'est marqué, et surtout rien
/// n'est reproché.
func testBurnTargetNotReachedStaysFalse() async throws {
    await closer.closeOpenDays(today: today)
    let log = try fetchDayLog(day2)
    XCTAssertFalse(log.burnTargetReached)
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `xcodebuild ... -only-testing:NivelTests/DayCloserTests test`
Attendu : ÉCHEC de compilation — `kcalBurned` inconnu.

- [ ] **Étape 3 : implémenter**

Sur `DayLog` :

```swift
// Défauts sur la DÉCLARATION (migration légère SwiftData). Renseignés par
// DayCloser à la clôture : c'est le seul moment où la dépense du jour est
// définitive (les pas peuvent encore monter jusqu'à minuit).
var kcalBurned: Int = 0
var burnTargetReached: Bool = false
```

Dans `GameService`, une méthode qui assemble les `BurnEntry` du jour depuis les `ActivityEntry` et appelle `BurnCalculator` avec les trois catalogues (`sessionCatalog`, `postureCatalog.sessions`, `MuscuCatalog.sessions`) fusionnés en un seul dictionnaire — **les trois, sinon une séance posture ou muscu retomberait sur le repli et ne serait jamais décomposée**.

Dans `DayCloser`, au moment de clore : calculer la dépense, l'écrire, et comparer à l'objectif (`profile.dailyBurnTarget` s'il est non nul, sinon `CalorieCalculator.dailyBurnTarget(weightKg:)`).

⚠️ **Passer les pas OPTIONNELS à `BurnCalculator`.** `DayCloser.close` a déjà les deux formes sous la main : `let steps = stepsByDay.map { $0[day] ?? 0 }` (un `Int?`) et `log.steps = steps ?? 0`. Passer `log.steps` enverrait **0** quand HealthKit est refusé, ce que `BurnCalculator` lit comme « les pas sont connus, ils valent zéro » : il exclurait alors les activités marchées, donc une journée de marche sans HealthKit afficherait une dépense nulle. C'est l'inversion exacte de la règle §5.4, et elle est invisible — aucune erreur, juste un zéro. Passer `steps`, tel quel.

- [ ] **Étape 4 : lancer les tests**

Run : `xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : SUCCÈS.

- [ ] **Étape 5 : commit**

```bash
git add App/Models/PersistentModels.swift App/Services/DayCloser.swift \
        App/Services/GameService.swift NivelTests/DayCloserTests.swift
git commit -m "feat(app): la dépense du jour est enregistrée à la clôture"
```

---

## Task 6 : le deuxième anneau

**Fichiers :**
- Modifier : `App/Views/Home/CalorieRing.swift`
- Modifier : `App/Views/Home/HomeView.swift`
- Test : `NivelTests/HomeDashboardTests.swift`

- [ ] **Étape 1 : implémenter l'anneau intérieur**

`CalorieRingCard` prend deux paramètres de plus et gagne son anneau intérieur :

```swift
struct CalorieRingCard: View {
    let eaten: Int
    let target: Int
    let burned: Int
    let burnTarget: Int

    private var burnFraction: Double {
        guard burnTarget > 0 else { return 0 }
        return min(1, Double(burned) / Double(burnTarget))
    }
    private var burnReached: Bool { burnTarget > 0 && burned >= burnTarget }
```

Dans `ring`, sous les deux cercles existants, l'anneau intérieur à ~70 % du rayon :

```swift
// Anneau de dépense (spec §5.5). `Theme.orange` et NON `Theme.accent` :
// l'anneau extérieur passe à `accent` en cas de dépassement calorique, et les
// deux cercles deviendraient alors indistinguables — précisément les jours où
// l'on regarde la carte de près. (La façade `Theme` n'expose pas `primary` :
// c'est `Theme.orange` qui porte `palette.primary`.)
Circle()
    .stroke(Theme.track, lineWidth: 8)
    .padding(14)
Circle()
    .trim(from: 0, to: burnFraction)
    .stroke(Theme.orange, style: StrokeStyle(lineWidth: 8, lineCap: .round))
    .rotationEffect(.degrees(-90))
    .padding(14)
```

Le centre **ne change pas** : le chiffre du mangé reste en grand.

La légende remplace le sous-titre existant plutôt que de s'y ajouter — la 1.13 s'est battue pour chaque point de hauteur :

```swift
@ViewBuilder private var subtitle: some View {
    VStack(spacing: 3) {
        if isOver {
            Text("Objectif dépassé de ~\((eaten - target).frFormatted), ça arrive 😌")
                .foregroundStyle(Theme.subtext)
        } else {
            HStack(spacing: 4) {
                Text("Reste ~\(max(0, target - eaten).frFormatted) kcal")
                CozyIcon(name: "tab_meals", size: 15)
            }
            .foregroundStyle(Theme.green)
        }
        HStack(spacing: 4) {
            Circle().fill(Theme.orange).frame(width: 7, height: 7)
            // Jamais de reproche au-delà de l'objectif : on a bougé plus que prévu,
            // il n'y a rien à redire.
            Text(burnReached
                 ? "Objectif de dépense atteint 🎉"
                 : "Dépensé ~\(burned.frFormatted) / \(burnTarget.frFormatted)")
        }
        .font(.caption2.weight(.semibold))
        .foregroundStyle(Theme.subtext)
    }
}
```

Et l'accessibilité : `accessibilityLabel("Calories : environ \(eaten) sur \(target). Dépensé : environ \(burned) sur \(burnTarget)")`.

Dans `HomeView`, un `@State private var burned: Int = 0` rafraîchi dans `refresh()` par la méthode de `GameService` de la Task 5, passé à `CalorieRingCard`.

**D'où vient `burnTarget`** : `profile.dailyBurnTarget` s'il est non nul, sinon `CalorieCalculator.dailyBurnTarget(weightKg:)` calculé sur la dernière pesée — exactement la même règle de repli que `DayCloser` (Task 5). L'écrire une fois dans `GameService` et la lire des deux côtés, plutôt que deux fois.

**Les previews** : `CalorieRing.swift` en contient **deux**, qui construisent `CalorieRingCard` directement — les deux cassent et prennent les deux nouveaux paramètres. Les trois de `HomeView.swift` passent par `homePreviewFixture`, qui est le seul endroit à toucher là-bas. Prévoir un cas de preview « dépense atteinte » pour voir la légende de réussite.

- [ ] **Étape 2 : lancer les tests**

Run : `xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : SUCCÈS.

- [ ] **Étape 3 : vérifier la contrainte de hauteur**

**Étape non négociable** : la 1.13 entière a servi à faire tenir l'accueil sans défilement. Ouvrir la preview « Accueil » et vérifier, dans cet ordre :
1. Taille de texte par défaut, iPhone 17 Pro → aucun défilement.
2. **Dynamic Type XXL** → aucun défilement, ou à défaut un défilement qui ne coupe pas la carte de séance.
3. iPhone SE (petit écran) → l'anneau intérieur reste lisible, les deux traits se distinguent.
4. Preview « Dépassé, sans HealthKit » → les deux anneaux ont bien **deux couleurs différentes**.

Si la hauteur ne passe pas, **ne pas rogner l'anneau** : réduire l'interligne de la légende ou replier l'objectif de dépense (« Dépensé ~180 » sans le « / 400 »).

- [ ] **Étape 4 : commit**

```bash
git add App/Views/Home/CalorieRing.swift App/Views/Home/HomeView.swift
git commit -m "feat(app): un deuxième anneau pour la dépense du jour"
```

---

## Task 7 : les quatorze badges

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/BadgeEngine.swift`, `Resources/badges.json`
- Modifier : `App/Services/GameService.swift` (`BadgeStats`)
- Test : `NivelCore/Tests/NivelCoreTests/BadgeEngineTests.swift`

- [ ] **Étape 1 : écrire les tests qui échouent**

```swift
func testCatalogueHasThirtyEightBadges() throws {
    let badges = try Catalogs.badges()
    XCTAssertEqual(badges.count, 38)
    XCTAssertEqual(Set(badges.map(\.id)).count, badges.count)
    XCTAssertEqual(Set(badges.map(\.title)).count, badges.count, "titres en doublon")
}

func testNewMetricsUnlock() {
    var stats = BadgeStats()
    stats.burnTargetDays = 10
    stats.muscuSessionsDone = 1
    stats.recipesLogged = 0
    let unlocked = BadgeEngine.newlyUnlocked(badges: try! Catalogs.badges(),
                                             stats: stats, alreadyUnlocked: [])
    XCTAssertTrue(unlocked.contains { $0.id == "burn_first" })
    XCTAssertTrue(unlocked.contains { $0.id == "burn_10" })
    XCTAssertTrue(unlocked.contains { $0.id == "muscu_first" })
    XCTAssertFalse(unlocked.contains { $0.id == "burn_30" })
    XCTAssertFalse(unlocked.contains { $0.id == "recipe_first" })
}

/// Un badge déjà obtenu ne se reprend jamais, même si la métrique redescend :
/// `badgeUnlocks` est un journal, pas un état recalculé.
func testAlreadyUnlockedIsNeverReturnedAgain() {
    var stats = BadgeStats()
    stats.burnTargetDays = 30
    let unlocked = BadgeEngine.newlyUnlocked(badges: try! Catalogs.badges(),
                                             stats: stats, alreadyUnlocked: ["burn_first", "burn_10"])
    XCTAssertFalse(unlocked.contains { $0.id == "burn_first" })
    XCTAssertTrue(unlocked.contains { $0.id == "burn_30" })
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter BadgeEngineTests`
Attendu : ÉCHEC — 24 badges, et pas de `burnTargetDays`.

- [ ] **Étape 3 : implémenter**

`Badge.Metric` gagne `burnTargetDays`, `muscuSessionsDone`, `recipesLogged` ; `BadgeStats` les trois propriétés et les trois cas de `value(for:)`. Les quatorze entrées de `badges.json` : **spec §5.6**.

**Les icônes sont le point serré de cette tâche.** `CatalogsTests` exige que **tous les badges aient une icône distincte** (`Set(badges.map(\.icon)).count == badges.count`) : la grille des badges s'affiche d'un bloc et s'en sert comme identité visuelle. Les 24 badges existants consomment déjà 24 des 50 noms du catalogue d'icônes, et une bonne part du reste est constituée de contrôles d'interface (`icon_play`, `icon_pause`, `icon_restart`, `icon_settings`, `icon_check`, les `tab_*_fill`) qui n'ont rien à faire sur un trophée.

Marche à suivre : d'abord lister les noms réellement libres (`ls App/Assets.xcassets/Icons`, moins ceux déjà pris par un badge), en attribuer autant que possible, et **passer les autres en `CatalogIcon.emoji`** — deux quêtes le font déjà (« 🍎 », « 🍏 »), c'est un cas prévu du modèle, pas un pis-aller. Ne jamais réutiliser l'icône d'un badge existant : le test refuse, et à raison.

Dans `GameService`, renseigner les trois compteurs :
- `burnTargetDays` : nombre de `DayLog` avec `burnTargetReached == true` ;
- `muscuSessionsDone` : `ActivityEntry` de `kind == .muscu` ;
- `recipesLogged` : repas dont au moins une ligne porte un item marqué `isRecipe` — **arrive au lot D**. En attendant, le laisser à 0 avec un `// TODO lot D` : les deux badges recette resteront simplement verrouillés, ce qui est exact.

- [ ] **Étape 4 : mettre à jour les deux valeurs épinglées**

Deux tests existants comptent les badges et leurs icônes, et **aucun des deux n'est dans la liste de fichiers ci-dessus par hasard** — il faut les y ajouter :

1. `NivelCore/Tests/NivelCoreTests/CatalogsTests.swift` : `badges.count == 24` → **38** (deux assertions, dont celle sur les ids uniques).
2. `NivelTests/IconAssetsTests.swift` : `cozyCount == 43`. **Le recompter, ne pas le deviner** — sa valeur dépend du partage cozy/emoji décidé à l'étape 3. Le test affiche le compte obtenu quand il échoue : le relever et le réécrire, en corrigeant aussi le commentaire qui détaille la répartition.

- [ ] **Étape 5 : lancer les tests**

Run : `cd NivelCore && swift test && xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : SUCCÈS.

- [ ] **Étape 6 : commit**

```bash
git add NivelCore/Sources/NivelCore/BadgeEngine.swift \
        NivelCore/Sources/NivelCore/Resources/badges.json \
        NivelCore/Tests/NivelCoreTests/BadgeEngineTests.swift \
        NivelCore/Tests/NivelCoreTests/CatalogsTests.swift \
        NivelTests/IconAssetsTests.swift \
        App/Services/GameService.swift
git commit -m "feat(core): quatorze badges, dont deux niveaux qui veulent enfin dire quelque chose"
```

---

## Task 8 : l'XP muscu et les quatre quêtes

**Fichiers :**
- Modifier : `NivelCore/Sources/NivelCore/XPEngine.swift`, `QuestEngine.swift`, `Resources/quests.json`
- Modifier : `App/Services/DayCloser.swift`, `App/Views/Onboarding/OnboardingFlow.swift`, `App/Views/Home/HomeView.swift` (fixture)
- Test : `NivelCore/Tests/NivelCoreTests/XPEngineTests.swift`, `QuestEngineTests.swift`

- [ ] **Étape 1 : écrire les tests qui échouent**

```swift
// XPEngineTests
func testMuscuSessionHasItsOwnDailyCap() {
    XCTAssertEqual(XPEngine.award(.muscuSessionDone, todayCount: 0), 40)
    XCTAssertEqual(XPEngine.award(.muscuSessionDone, todayCount: 1), 0)
    // Trois séances de nature différente le même soir paient TROIS fois : les
    // plafonds sont indépendants, sinon le lot punit ce qu'il veut installer.
    XCTAssertEqual(XPEngine.award(.dailySessionDone, todayCount: 0)
                   + XPEngine.award(.postureSessionDone, todayCount: 0)
                   + XPEngine.award(.muscuSessionDone, todayCount: 0), 120)
}

// QuestEngineTests
func testMuscuQuestsAreDrawnOnlyWhenAvailable() throws {
    let pool = try Catalogs.quests()
    let without = QuestEngine.weeklyDraw(pool: pool, weekID: "2026-W33",
                                         stepsAvailable: true, postureAvailable: false,
                                         muscuAvailable: false)
    XCTAssertFalse(without.contains { $0.requiresMuscu })

    let with = QuestEngine.weeklyDraw(pool: pool, weekID: "2026-W33",
                                      stepsAvailable: true, postureAvailable: false,
                                      muscuAvailable: true)
    XCTAssertEqual(with.count, without.count, "le tirage garde sa taille")
}

func testQuestCatalogueHasTwentyFive() throws {
    let quests = try Catalogs.quests()
    XCTAssertEqual(quests.count, 25)
    XCTAssertEqual(Set(quests.map(\.id)).count, quests.count)
}
```

- [ ] **Étape 2 : lancer, vérifier l'échec**

Run : `cd NivelCore && swift test --filter XPEngineTests`
Attendu : ÉCHEC — `muscuSessionDone` inconnu.

- [ ] **Étape 3 : implémenter**

Dans `XPEngine`, le cas et son barème, avec le commentaire qui explique le plafond indépendant (calquer celui de `postureSessionDone`, qui dit déjà pourquoi).

Dans `QuestEngine` : `requiresMuscu` sur `Quest` (défaut `false` au décodage, comme `requiresPosture`), paramètre `muscuAvailable` sur `weeklyDraw`, filtrage identique. Les quatre quêtes de la **spec §5.7** dans `quests.json`, avec la métrique `muscuSessionsDone` et `burnTargetDays` sur `QuestEngine`.

Passer `muscuAvailable:` aux **quatre** appelants, faute de quoi l'arbre ne compile pas :

| appelant | valeur |
|---|---|
| `App/Services/DayCloser.swift` | `MuscuPlanSettings.shared.isEnabled` |
| `App/Views/Onboarding/OnboardingFlow.swift` | `MuscuPlanSettings.shared.isEnabled` |
| `App/Views/Quests/QuestsView.swift` | `MuscuPlanSettings.shared.isEnabled` |
| `App/Views/Home/HomeView.swift` (fixture de preview) | `false`, comme pour la posture |

`QuestsView` est facile à oublier : c'est le seul des quatre qui n'apparaît dans aucun test.

Éteindre l'interrupteur en cours de semaine ne touche **ni** `activeQuestIDs` **ni** `questProgress` : une quête devenue inatteignable reste à sa progression et le lundi suivant en tire une autre (règle posée par la posture, §7.2 de sa spec).

- [ ] **Étape 4 : re-épingler le tirage, et consigner pourquoi**

`QuestEngineTests.testWeeklyDrawIsPinnedForKnownWeek` fixe les trois ids tirés pour une semaine connue. Son commentaire porte une promesse de la v1.11 : « le tirage de Michaël doit rester identique malgré les 3 quêtes posture ajoutées au pool ». Cette promesse tenait parce que ces quêtes sont **filtrées avant le mélange** quand le programme est éteint.

`burn_target_3` **ne porte aucun drapeau** : elle entre dans le pool éligible de tout le monde, qui passe de 18 à 19 entrées, et le mélange change. Ce n'est pas une régression — **ajouter une quête sans drapeau est censé changer les tirages futurs**, c'est même tout l'intérêt. Mais il faut le dire :

1. Relever les trois nouveaux ids que le test affiche en échouant, et **re-épingler** — jamais re-dériver la valeur attendue depuis le code, ce qui viderait le test de son sens.
2. Compléter son commentaire : la stabilité promise en v1.11 ne valait que pour les quêtes **à drapeau**, et la 1.14 ajoute la première quête sans drapeau depuis.
3. Dans `CatalogsTests`, `quests.count == 21` → **25**, et l'assertion `filter(\.requiresPosture).count == 3` reste vraie (les trois nouvelles quêtes muscu portent `requiresMuscu`, pas `requiresPosture`).

- [ ] **Étape 5 : lancer les tests**

Run : `cd NivelCore && swift test && xcodebuild -project Nivel.xcodeproj -scheme Nivel -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
Attendu : SUCCÈS.

- [ ] **Étape 6 : commit**

```bash
git add NivelCore/Sources/NivelCore/XPEngine.swift \
        NivelCore/Sources/NivelCore/QuestEngine.swift \
        NivelCore/Tests/NivelCoreTests/CatalogsTests.swift \
        App/Views/Quests/QuestsView.swift \
        NivelCore/Sources/NivelCore/Resources/quests.json \
        NivelCore/Tests/NivelCoreTests/XPEngineTests.swift \
        NivelCore/Tests/NivelCoreTests/QuestEngineTests.swift \
        App/Services/DayCloser.swift App/Views/Onboarding/OnboardingFlow.swift \
        App/Views/Home/HomeView.swift
git commit -m "feat(core): l'XP des séances muscu et quatre quêtes de plus"
```

---

## Task 9 : vérification du lot

- [ ] **Étape 1 : suite complète**

```bash
cd NivelCore && swift test && cd ..
xcodebuild -project Nivel.xcodeproj -scheme Nivel \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test
```

- [ ] **Étape 2 : les deux suites qui n'ont pas droit à l'erreur**

```bash
xcodebuild ... -only-testing:NivelTests/LevelMigrationTests test
cd NivelCore && swift test --filter BurnCalculatorTests
```
Attendu : SUCCÈS. **Un échec ici n'est pas un détail** : la première fait perdre des niveaux, la seconde affiche un chiffre faux sans jamais le dire.

- [ ] **Étape 3 : essai sur un store existant**

Installer la version précédente sur le simulateur, jouer assez pour atteindre le niveau 4 ou 5, puis installer par-dessus la version du lot. Vérifier que **le niveau affiché n'a pas baissé** et que le total d'XP a bien fait son bond. C'est le seul essai qui valide la migration légère SwiftData sur un vrai store.

- [ ] **Étape 4 : relecture de la spec**

Reprendre **spec §5** : formule, `legacyXPRequired` intacte, migration une seule fois, objectif borné et arrondi, pas de double comptage, séances décomposées, deux anneaux de couleurs distinctes, aucun rouge, 38 badges, 25 quêtes, widget inchangé.

**Ce que le lot C ne fait PAS** : le widget garde son anneau unique (spec §5.5), les kcal de dépense ne sont jamais créditées au budget alimentaire, et `recipesLogged` reste à 0 jusqu'au lot D.
